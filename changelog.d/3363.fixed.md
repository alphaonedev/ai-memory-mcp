Bind the caller principal on the last eight MCP tools that still trusted a
wire-asserted `agent_id`: `memory_agent_register` (`caller_agent_id`),
`memory_skill_retire`, `memory_skill_delete`, `memory_skill_get`,
`memory_skill_promote_from_reflection`, `memory_check_agent_action`,
`memory_action_create` and `memory_routine_create` (`created_by`). Under the
multi-tenant posture (`AI_MEMORY_AGENT_ID` set) a differing wire principal is
now refused instead of being written into the forensic audit chain, the
`retired_by`/`purged_by` provenance columns, the `skill.invoked` signed-event
actor or the coordination quota key, and a wire `agent_id` is always validated.
The single-operator default (env unset) is unchanged.
