**#3587 U5b — docs/SSOT for U5a + U2 (no product bytes).**

Operator-facing contract for the shipped swarm anti-drift units:

- `docs/CLI_REFERENCE.md` § `watch`: `--host file:<path>` row, path
  safety, per-file `transcript_line_dedup`, `observed_actor`,
  `SWARM_LINE_TAGS`, `refuse_pg_store` / SAL write, parent-directory
  `fs-notify` with poll fallback. Corrects the stale "notify crate is
  forbidden" claim (#1978 already shipped the opt-in feature).
- `docs/anti-drift.md`: swarm line-file capture (inbox/outbox), two
  truth-sets boundary, what has **not** shipped (U1/U3/U4/U6).
- `docs/CONFIG_SCHEMA.md` + `docs/ENGINEERING_STANDARDS.md` §2.7:
  QUAL-10 lockstep ceilings from U5a (`storage/mod.rs` 35_200,
  `config.rs` 15_500, `install.rs` 3_900). Not runtime config keys.
- `docs/README.md` index entry.

No `src/` changes. No schema bump. Later U5b follow-up covers
capture-turn / curator / `ruling_key` / `resolve` when those units land.
