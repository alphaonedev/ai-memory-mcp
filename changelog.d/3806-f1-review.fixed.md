**#3806 — f1's adversarial security review of the pre-rebase `[decision]` branches (8 findings), resolved on the integrated stack.** A missing chat envelope (a 200 with no `choices[0].message.content`, or a SystemOne body with no `decision`) is now an outage (`unavailable`, where `fallback` governs) rather than a model decline; an explicit `message.refusal` stays a decline. The one-shot `logprobs` retry spends what is LEFT of `[decision].timeout_secs`, so a logical decision call never exceeds its budget. `[decision]` endpoints render as `scheme://host[:port]` in every `Debug`, `Display` and log sink, so URL userinfo and query credentials never print. The calibration producer exits non-zero on a FAIL or PARTIAL report (after writing it), and a corpus that does not cover every seam plus a negative control reports PARTIAL, never PASS. Redirects, the boot-refusal path under `fallback = "refuse"` and the generative second-hop transport classification were already fixed on this stack and are now pinned by f1's own probes.
