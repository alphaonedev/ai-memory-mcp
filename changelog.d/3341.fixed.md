Fix HTTP GET-by-id on postgres so it uses the per-anchor `get_links_for_anchor`
SAL surface instead of scanning the entire `memory_links` table via
`list_links(None)` on every GET. Scale the compiled-default Postgres pool
(`max_connections`) and the sqlite WAL read-pool with host cores so
concurrent keyword reads are not capped at 16 / 8 in-flight queries.
