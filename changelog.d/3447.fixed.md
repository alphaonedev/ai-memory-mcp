Python SDK CI now discovers both the core and swarm test suites by default, and the bounded-concurrency regression uses deterministic ordering/max-in-flight assertions instead of a wall-clock deadline.
