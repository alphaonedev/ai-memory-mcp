**Fixed: `ai-memory wrap codex` fails closed outside the tested Codex CLI range (#3545 U1).**

Codex CLI `>= 0.153.0` rejects the wrapper's default `--system` flag
(`error: unexpected argument '--system' found`, exit 2). `wrap` now
probes `{agent} --version` for `codex` / `codex-cli` against a
checked-in table (`CODEX_WRAP_TESTED_RANGE` = `< 0.153.0`;
`CODEX_WRAP_KNOWN_BROKEN` = `>= 0.153.0`) and refuses with a message
that names `--system-flag`, unless the caller passed `--system-flag` /
`--system-env` / `--message-file-flag`. Unparseable version output
is also refused (fail closed). Docs rewritten from the same table
(pinned by `wrap_codex_docs_cite_tested_range_ssot_3545`).
Native MCP remains the supported path on known-broken versions.
The default strategy mapping is unchanged; repairing `--system` for
0.153+ is certificate work (G2).

#3545 is split (Conductor 2026-09-11T22:48:36Z): this fragment is **U1**
(tag-side fail-closed + table + docs + fake-codex tests). **U2** (boot
sentinel: injected marker echoed in the first turn, absent → WARN
naming host+version, or `doctor --host` self-test) lands after U1
review on the same lane.
