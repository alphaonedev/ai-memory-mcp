Graph and family read funnels apply the canonical substrate visibility rule on SQLite and PostgreSQL. Naming a row ID explicitly admits a readable anchor, including an inbox row for its owner or target; reached nodes, non-anchor link endpoints, and path intermediates remain ambient and withhold substrate rows. HTTP timeline preserves the ordinary-row owner gate and its 403 response, while inaccessible substrate anchors return 404. HTTP link target admission refuses inaccessible substrate rows and preserves source-owner authority over ordinary targets, including foreign-owned and legacy-unowned rows. MCP retains its configured-caller target-visibility check. Regression coverage includes SQLite and live PostgreSQL.
