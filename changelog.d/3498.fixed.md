Graph and family read funnels now apply the canonical substrate-namespace visibility rule on SQLite and PostgreSQL. Ambient reads withhold inboxes, agent registries, sessions, and standards; explicitly named inbox reads retain the caller ownership check. ID-anchored traversals filter returned nodes, link endpoints, and path intermediates before serialization. Added SQLite regressions and live-PostgreSQL graph and HTTP recall/search/list matrices.
