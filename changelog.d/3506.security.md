Enforce owner-only routine execution before MCP run/audit writes and both SAL materialization paths. Refuse ownerless routines for enforced callers; bind run metadata, actions, audits and quota charges to the resolved caller. Admin visibility and as_agent do not grant execution. Preserve the local single-operator identity ladder. Add SQLite/live-PostgreSQL denial and attribution regressions and an explicit missing-PG skip marker (#3506).
