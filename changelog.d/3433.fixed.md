CLI `notify`, `subscribe`, and `inbox` now honor the globally resolved
`--agent-id`: writes stamp and authorize the intended caller, while the
separate `--inbox-agent-id` selector is bound to that caller and refuses
cross-agent reads.
