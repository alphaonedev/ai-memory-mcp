#3715 items 3 + 4 — `ai-memory config show --effective [--provenance]` prints the configuration as the boot loader accepts it (same funnel as `serve`: unknown and removed keys refuse with `EX_CONFIG`), secrets masked through the #3432 funnel, with a per-key provenance manifest (`file <path>` / `compiled-default`, deprecation state) and the `AI_MEMORY_*` variables present by name. The key deprecation lifecycle is now data (`config::deprecated_keys::DEPRECATED_KEYS`: key, replacement, deprecated-in, removed-in): the loader WARNs per deprecated key naming its replacement and REFUSES a removed key naming its replacement. The v1 flat keys are recorded as deprecated since 0.7.0 with removal not scheduled; the pre-existing WARN that promised removal in v0.8.0 now says so.
