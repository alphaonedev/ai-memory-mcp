**Wake-hub process isolation: distinct `ai-memory-hub` user, store jail, AF_UNIX only (#3578 items 3–5).**

The shipped `ai-memory-wake-hub.service` now runs as `User=ai-memory-hub` (not the daemon's `ai-memory`), jails the durable store with `InaccessiblePaths=/var/lib/ai-memory`, and keeps `RestrictAddressFamilies=AF_UNIX` with no `--tcp` in ExecStart. The #3504 refresher still opens the store as `User=ai-memory` and `install(1)`s the snapshot 0600 owned by `ai-memory-hub` into `/run/ai-memory-hub/` (the hub's `AllowlistFile` gate requires that uid + mode). The launchd template names `UserName`/`GroupName` `ai-memory-hub` (honoured only as a LaunchDaemon). Packaging pins live in `tests/wake_hub_process_isolation_3578.rs` (allowed + denied fixtures). Threat-model rows for the four sidecar exposures land in the certification standard §0.6 and `SECURITY.md`; red-team acceptance cases are listed on #3473 / `scripts/bench/README.md`.
