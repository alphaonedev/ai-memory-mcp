API_REFERENCE.md HTTP sweep matches the live surface (#3416).

`GET /api/v1/stats` no longer documents a universal `total_memories` /
`storage_backend: "sqlite"` envelope: sqlite serializes `Stats.total`
with no backend marker; postgres aliases `total_memories` and adds
`storage_backend: "postgres"`. Tool/route counts now cite the SSOT
(`Profile::full().expected_tool_count()` = 104 advertised entries at
`--profile full`; `EXPECTED_PRODUCTION_UNIQUE_PATHS_COUNT` = 86;
`EXPECTED_PRODUCTION_ROUTES_COUNT` = 100) instead of the stale 103 /
84 / 98 / 80-unique-path figures. `POST /api/v1/namespaces` is
documented as body-only (query-string `?namespace=` is not read);
DELETE remains the query-string collection verb. Pinned by
`tests/api_reference_http_sweep_3416.rs` and
`http_set_namespace_standard_qs_query_string_is_not_read_3416`.
