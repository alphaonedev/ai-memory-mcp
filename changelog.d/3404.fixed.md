SQLite `search` and `recall` (FTS and semantic) now project the one canonical
memory row list shared with PostgreSQL, so `version`, `cid`, lifecycle,
VALID-time and `confidence_source` fields come from the stored row instead of
mapper defaults.
