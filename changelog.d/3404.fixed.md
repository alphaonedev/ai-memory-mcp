SQLite `search` and `recall` now project the same canonical memory columns as
`get`, preserving durable `version`, `cid`, lifecycle, bitemporal, and
confidence-provenance fields instead of silently fabricating mapper defaults.
The canonical projection is shared with PostgreSQL, preventing backend drift.
