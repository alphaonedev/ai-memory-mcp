API_REFERENCE.md memory/graph sweep matches the live surface (#3428).

`POST /api/v1/memories` `kind` now lists all 16 `MemoryKind` slugs
(`MemoryKind::all()`), including Goal/Plan/Step (#1709) and
Told/Instruction/Intervention (#1945); the CLI `--kind` row matches.
Bulk no longer claims a universal HTTP 200 / "#2588 until that lands"
contract: #2588 shipped (`src/handlers/bulk.rs`) as 200 / 207 / 202 /
dominant-cause, and a wholly quota-rejected batch is 429.
`DELETE /api/v1/links` is documented as `{"deleted": true}` (bool), with
a request/response section; `POST /links/verify`, `POST /kg/find_paths`
(and the registered `/find_paths` alias), and
`GET /memories/{id}/lineage` gained the same. `POST /entities` discloses
there is no `entity_type` field. CLI `--kind` and USER_GUIDE `kind` rows
match the 16-slug SSOT. Pinned by
`tests/api_reference_memory_graph_sweep_3428.rs`.
