PostgreSQL `vector(N)` conversion now preserves the v96 embed-skip trigger.
`migrate_embedding_dim` lets its embedding-NULL update invalidate skip-cache
rows, drops the column-dependent trigger only around the type alteration, and
recreates it before the transaction commits. A failure rolls back the column
and trigger changes together, and the existing record-stop gate remains ahead
of all conversion DDL.
