Close an unauthenticated cross-tenant write in `memory_auto_tag`. The tool took
no caller at all: it read the target row through the unfiltered by-id read,
shipped another agent's `scope=private` title and content to the external
model, and wrote the resulting tags back through the raw storage primitive —
bypassing the owner gate, the permission rules and the namespace governance
funnel that `memory_update` enforces, so a `governance.write = "approve"`
namespace queued a pending action for `memory_update` while `memory_auto_tag`
committed immediately. The read is now masked by the same caller-scoped
visibility funnel `memory_get` uses, before any model call, and the write is
delegated to the governed update path, so auto-tagging inherits the owner gate,
the permission decision, the governance decision (including the `pending`
approval envelope, which is now surfaced instead of a success body reporting
tags that were never persisted) and the audit trail by construction. On HTTP,
`POST /api/v1/auto_tag` gained the same visibility gate on the sqlite branch
(the postgres branch was already filtered) plus the per-agent-key identity gate
that `POST /api/v1/consolidate` already had. The single-operator default
(`AI_MEMORY_AGENT_ID` unset) is unchanged.
