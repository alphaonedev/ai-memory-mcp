`rules keygen --out <path>` outputs are now discoverable by the operator-key loader when exactly one custom keypair is present, with ambiguous candidates refused fail-closed. `agents bind-api-key` adds mutually exclusive `--token-file <path>` and `--token-file -` stdin secret channels; Unix token files must be mode 0600.
