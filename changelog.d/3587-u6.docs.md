**#3587 U6 PREP — watch line-file daemon operator wiring (no activation, no product bytes).**

Copy-and-substitute artifacts for the swarm line-file `watch --daemon`:

- `docs/ops/ai-memory-watch.service`: systemd user unit (f2).
  `watch --daemon --host file:<inbox> --host file:<outbox> --agent-id <principal>`,
  `Restart=on-failure`, `NoNewPrivileges`, `ProtectSystem=strict`,
  `ReadWritePaths=<db-dir>` only, `PrivateTmp`, `UMask=0077`.
- `docs/ops/co.alphaone.ai-memory-watch.plist`: launchd agent (f1),
  same argv, crash-restart `KeepAlive`, logs under
  `<home>/Library/Logs/`, `Umask` 077.
- `docs/ops/watch-wiring.md`: install / enable / disable for both
  hosts, Conductor MCP `AI_MEMORY_AGENT_ID`, two-truth-sets boundary,
  WAL + bounded retry, verification (`watch --once --dry-run` first).

Placeholders only. Activation stays with the Conductor. No `src/`
changes. No schema bump.
