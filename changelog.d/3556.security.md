### Security

- **[ci] cert-expiry gate watches the §5 surface, refuses a VOID
  banner, and fails on bind-SHA ancestor drift**
  ([#3556](https://github.com/alphaonedev/ai-memory-mcp/issues/3556)).
  `scripts/check-cert-expiry.sh` now watches `src/identity/**`,
  `src/storage/migrations.rs`, `src/store/postgres.rs` (and
  `src/store/postgres/**`, the B7 write plane), and
  `src/handlers/admin.rs` in addition to the federation-wire set. A
  STATUS banner of VOID/EXPIRED fails unless the same change touches
  the cert doc. `git diff <bind-sha>..HEAD` over the watch set
  nonempty fails until the cert is re-issued. `--self-test` plants
  the new paths, the VOID banner, and the ancestor drift.
