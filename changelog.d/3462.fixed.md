Regenerated `conformance/vectors/export/round_trip_l3.json` for the #3446 canonical CID pre-image. #3446 deliberately changed how `created_at` enters the cid pre-image (the instant, not its per-backend rendering) but did not regenerate the pinned export golden, so `tests/conformance_export_roundtrip_2030` failed on every CI leg after it merged. Only the pinned `cid` value changed; the exporter itself is untouched. Regenerated with `AI_MEMORY_REGEN_GOLDEN=1 cargo test --test conformance_export_roundtrip_2030` and re-verified against `tests/cid_created_at_canonical_3446.rs`.
