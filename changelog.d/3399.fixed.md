Preserve the authenticated HTTP caller exactly as the recorded notification sender on SQLite, matching PostgreSQL and preventing distinct caller identities from collapsing through MCP client-name synthesis.
