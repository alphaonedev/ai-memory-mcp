**Postgres tool parity (#3064, lane L-PGP) — the bare `/api/v1/find_paths`
alias is served on postgres.** `src/lib.rs` wires BOTH `routes::FIND_PATHS`
and the already-pg-supported `routes::KG_FIND_PATHS` to the SAME handler
(`handlers::kg_find_paths`, which takes the `StorageBackend::Postgres`
branch into `MemoryStore::find_paths` and never `app.db.lock()`), so the
alias was refused only because it was missing from
`postgres_endpoint_supported` — a postgres daemon 501'd a route whose
byte-identical twin it already served. No new SAL surface. Inventory gate
`tests/pg_supported_route_inventory_gate_2799.rs`: 66 -> 67 pg-supported /
17 -> 16 fully-501 / 83 unique paths. Proof-of-dispatch on BOTH backends:
`tests/pg_parity_3064.rs`.
