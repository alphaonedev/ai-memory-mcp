**Removed: one LLM provider alias (#3627).**

An explicit `AI_MEMORY_LLM_BACKEND` / `[llm].backend` of the retired
token is now refused with the standard unknown-alias error naming the
accepted selectors (no silent fallback). Current docs, alias tables,
vendor-literal gate, and the v1.0.0 capabilities inventory drop the
row. Historical release records stay. The retired token is named in
the CHANGELOG Removed entry.
