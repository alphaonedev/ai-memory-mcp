**#3549 (SECURITY, ga-blocker) — ONE caller-authority resolver beneath every
handler.** `identity::authority::Authority { principal, binding, admin }`
(private constructor) is resolved exactly once per request at the two dispatch
chokepoints — the MCP `tools/call` arm BEFORE the `TOOL_DISPATCH_TABLE` lookup
(carried as the non-optional `ToolDispatchCtx.authority`) and the new HTTP
`authority_layer` composed tower-inside `api_key_auth` (attached as a request
extension). Fail-closed at the chokepoint: an unusable configured
`AI_MEMORY_AGENT_ID` refuses every MCP tool call (`-32603`), and a present but
malformed / reserved `X-Agent-Id` is refused with a typed `400` on every HTTP
route before any handler runs (probes and the federation `/sync/*` boundary are
the checked-in exemptions with reasons). No `decision` field, no K9 `Op`
growth: object predicates stay per handler, and `visibility::is_visible_to_caller`
is retired as a public predicate — every former call site goes through
`is_readable_on_query`. `tests/authority_boundary_structural_3549.rs` is a
positive inventory (no `skip_path`) over every table entry and every `.route()`
registration, with `tests/authority_boundary_3549_allowlist.txt` frozen as the
boundary spec; the read-funnel leg of that inventory surfaced six ungated MCP
reads, filed as #3596–#3601. Rulings recorded in `SECURITY.md`: #3125 (Standard
posture is `enforce` with `Allow` absent rules; ungoverned namespaces stay
allow-on-silence, the strict knob is posture-gated) and F13 (MCP stdio is ONE
trust domain; multi-principal only over HTTP with per-agent keys).
