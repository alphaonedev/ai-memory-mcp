**CI: pin GITHUB_TOKEN to `contents: read` on eight workflows (#3591).**

Closes CodeQL `actions/missing-workflow-permissions`. Workflow-level default is `contents: read` on `ci.yml`, `c8-precheck.yml`, `batman-mode-acceptance.yml`, `coverage.yml`, `fuzz.yml`, `session-boot-lifetime.yml`, `token-budget.yml`, and `tool-count-drift.yml`. Two jobs widen: `c8-precheck.yml` `external-pr-operator-approval-gate` adds `pull-requests: read` for the `gh api …/reviews` step; `coverage.yml` `per-module-thresholds` keeps `pull-requests: write` for `marocchino/sticky-pull-request-comment`.
