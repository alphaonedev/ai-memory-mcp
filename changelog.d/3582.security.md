Behaviour change: a node without `AI_MEMORY_FED_PEER_ATTESTATION` now refuses all inbound federated namespace writes and catchup pulls by default; configure per-peer `allowed_namespaces`, or (Standard posture only) set `AI_MEMORY_FED_REQUIRE_PUSH_NAMESPACE_SCOPE=0` to keep legacy replication.

The shared write, by-id and namespace-metadata checks cover SQLite and PostgreSQL, including catchup, archive/restore, pending decisions and checkpoints. Declared scopes and configured-map enrollment refusals remain enforced.

Explicit federation peers without an allowlist, malformed allowlists and unreadable federation bindings refuse `asi-hard` boot; Standard warns. A valid `{}` declares no peers and permits boot while refusing inbound replication. Shared identity key enrollment or enrollment read errors only warn in both postures, because local agents use the same directory. Key enrollment establishes identity, not namespace permission. Doctor reports these separate observations; HTTP/MCP capabilities v2/v3 and remote doctor carry the daemon boot snapshot. v1 remains unchanged. No new environment knob or schema change. Enterprise-federation certification remains VOID pending Conductor re-validation.
