Federation namespace authorization now fails closed when the default-required scope check has no peer allowlist. The shared write, by-id and namespace-metadata checks cover SQLite and PostgreSQL, including catchup, archive/restore, pending decisions and checkpoints. Standard retains the existing explicit require-scope `0` opt-out; declared scopes and configured-map enrollment refusals remain enforced.

With outbound peers or inbound enrollment/bindings configured, `asi-hard` requires a valid, nonempty `AI_MEMORY_FED_PEER_ATTESTATION` and refuses boot without it; Standard warns. Incomplete inbound observation also refuses hardened boot. Key enrollment establishes identity, not namespace permission. Ordinary doctor remains runnable and reports local inbound facts while marking outbound/listener argv unobservable. HTTP/MCP capabilities v2/v3 and remote doctor carry the daemon's completed boot snapshot; v1 is unchanged. No new environment knob or schema change. Enterprise-federation certification remains VOID pending Conductor re-validation.
