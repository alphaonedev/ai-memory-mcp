Require the resolved MCP caller to belong to `[admin].agent_ids` (or
`AI_MEMORY_ADMIN_AGENT_IDS`) before honoring `memory_archive_purge as_admin:true`.
An empty allowlist admits nobody; the allow/refuse decision is audited.
Configured invalid identities fail closed before purge or GC side effects.

Under the configured-caller posture, `memory_gc` now collects only rows authored
by or addressed to a non-admin caller. Its preview, namespace-governance probe,
archive copies, link snapshots, expiry revisions, erasures and deletion share
one owner predicate; each write chunk remains atomic. Allowlisted admins and
the legacy single-operator posture retain the complete sweep. Unrelated signal,
observation and bind-challenge pruning remains on complete sweeps. Existing
recall TTL folding still precedes expiry collection. MCP dispatch is SQLite;
HTTP GC remains admin-only on SQLite and PostgreSQL, and HTTP archive purge
retains its checked admin escalation and caller scope.
