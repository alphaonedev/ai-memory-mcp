**#3595 — enterprise-federation certification re-issued after #3582.**

`docs/compliance/ENTERPRISE-FEDERATION-CERTIFICATION.md` is LIVE as of
2026-09-11 against `ad60beadf602823c4451ff82067f62091aba9a04`. The #3582
wire contract (absent `AI_MEMORY_FED_PEER_ATTESTATION` ⇒ inbound namespace
writes and catchup pulls refused by default; `{}` deny-all; `asi-hard`
boot refusal without a usable allowlist) is now the certified boundary.
Operator upgrade: set that map before deploying a federated node.
Evidence: `docs/compliance/evidence/cert-3595/` (both backends; zero
`skip:`; zero `FAILED`). No product bytes. No schema bump.
