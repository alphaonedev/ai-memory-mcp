`docs/schema.html` gains the per-rung row for schema **v95**
(`attested_write_ledger`, #3419), which the merged fix omitted: the ladder table
went from v94 straight to `</tbody>`, and the "Current =
CURRENT_SCHEMA_VERSION" marker was still pinned to the v94 rung. The marker now
sits on v95, matching the shipped `CURRENT_SCHEMA_VERSION`. Documentation only —
no code, schema or wire change.
