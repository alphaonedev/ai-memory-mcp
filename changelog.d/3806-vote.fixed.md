**#3806 — the remaining 5-agent vote (4d3ea1c5) changes.** R5: the decision metric series take a closed `DecisionOutcome` (and the preregistered seam) instead of three bare strings, so their cardinality is bounded by the type rather than by what a caller passes, and a reason label can exist only on an abstain. R6: a provider that cannot answer a question a wired seam asks (`choose` for `classify_kind`, `judge` for `detect_contradiction`) is refused by name at the boot chokepoint instead of abstaining `unsupported` on every call. R8: the decision lane's signed egress-refusal row is appended to the existing, schema-current store without a migrating open, so the chokepoint (which runs on every SIGHUP, MCP config change and CLI curator run) never replays DDL against a live store or creates a database at the configured path.
