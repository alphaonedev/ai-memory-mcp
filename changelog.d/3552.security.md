### Security

- **[ci] certified-pin jobs `cert-postgres-age` / `postgres-ignored` are
  now merge-blocking, with a non-vacuity ratchet**
  ([#3552](https://github.com/alphaonedev/ai-memory-mcp/issues/3552)).
  The jobs `Certified pg+AGE cells (live PG 18.6 + AGE 1.8.0 + pgvector
  0.8.6)` and `Postgres ignored tests (sal-postgres --ignored)` are
  declared required contexts (rule (f) now covers their workflow
  files) and pinned to live branch protection via the #3554
  `--pin-from-live` ratchet. Each cert leg lists tests under the
  claimed features, then FAILS if executed < listed, if executed <
  the committed floor
  (`scripts/qc-allowlists/cert-leg-executed-ratchet.txt`; lowering
  needs an issue-linked allowlist entry), or if any `skip:` line
  appears (live_pg() connect failure is FAIL in a cert leg — #3247
  item 3 / #3298). Same-repo-only job-level `if:` stays (forks cannot
  use `linux-fed`) and is listed in the (b2) fail-open ledger.
