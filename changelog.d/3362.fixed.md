Generic memory create, update, and promote surfaces now reject every `_`-prefixed system namespace, preventing callers from forging inbox and agent-registry rows while purpose-built system funnels continue to own those namespaces.
