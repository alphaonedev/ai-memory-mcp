**#3587 U5a — QUAL-10 lockstep ceilings + `field_names::RULING_KEY` SSOT (no behaviour).**

Pre-emptive module-size room so later swarm anti-drift units do not fight QUAL-10 (audit-C F12; 1x3 adjudication 2026-09-10):

- `src/storage/mod.rs` 34_000 → 35_200 (U1 `archive_as_superseded` + U2 consumers). MEASURED 33_848.
- `src/config.rs` 15_120 → 15_500 (U3 `[curator]` stale-ruling keys + U1 `[autonomy]` propose room). MEASURED 15_028.
- `src/cli/install.rs` 3_600 → 3_900 (U4 `--hook capture` + Codex capture leg). MEASURED 3_558.

QUAL-6 stays 132. New `crate::models::field_names::RULING_KEY` (`"ruling_key"`) is the SSOT for U1; this unit does not join the preserved-keys set and does not change any write path.

Acceptance: `tests/qual_10_module_size_ceiling.rs` (the three bumped rows). Later units land their own `changelog.d/3587-uN.*.md` fragments when they ship.
