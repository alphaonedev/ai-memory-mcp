`memory_notify` now charges each notification's row and payload bytes to the authenticated sender's per-namespace quota on SQLite, matching PostgreSQL, and refuses over-quota delivery before an inbox row can be written.
