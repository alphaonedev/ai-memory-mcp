**Cert-expiry gate reads the certificate it guards (#3556).**
`scripts/check-cert-expiry.sh` measured only the shape of a PR diff ("wire
surface touched without touching the cert doc"), which left two holes,
both measured on the v1.0.0 promotion tip: any edit of the cert doc
satisfied the hatch, so a range that rewired federation plus an incidental
prose edit passed with the STATUS and Binds-to lines untouched; and the
banner was never read, so a document saying LIVE bound to a SHA twelve
wire-file changes ago stayed green on every later PR — expired by its own
§7 while claiming otherwise. The gate now (B) accepts a cert-doc edit only
if the STATUS line or the Binds-to line changed, and (C) fails any HEAD
whose banner says LIVE bound to `<sha>` while §7-watched paths or
`AI_MEMORY_FED_*` identifiers changed between `<sha>` and HEAD, naming the
drift; VOID/EXPIRED makes no claim and never fails (C); drift is a tree
comparison, so ancestry is not required (a squash-merge with an equal
watched tree passes; a bind to an unrelated commit reds on its drift); the banner is read
by a formatting-tolerant pattern (blockquote, heading level, dash style,
spacing and hex case), must match exactly once (a decoy line above the real
banner fails closed), and a cert doc deleted alongside a wire change fails
closed. Self-test grows by thirteen cells (q–z2);
measured on real history, the promotion range at f32c18dad and an
unrelated change on top of it both go RED, the re-issued tip 562e61c82
and a change on top of it stay GREEN.
