Test-only: the qual_10 module-size ceiling for `src/mcp/mod.rs` is raised once for the v1.0.0 fix wave (17150 → 17400) so the branches adding dispatch arms stop bumping it privately with conflicting values. The module split remains tracked separately.
