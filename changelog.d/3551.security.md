Reflection export (MCP and HTTP, including native PostgreSQL HTTP) now resolves the caller and admits the reflection and every `reflects_on` source before rendering. HTTP checks the existing per-agent identity binding; an admin read exemption requires both explicit #3383 enrollment and the trusted/identity-bound admin predicate. The synthetic export admin principal and its C8 allowlist entry are removed. Unscoped substrate namespaces remain excluded.

SQLite skill promotion admits the same source set before rendering, promotion audit, signing or registration. HTTP retains its admin-only gate and attributes `metadata.promoted_by` to the resolved request actor. Contract change: hidden and missing members both return the generic `reflection not found: <requested reflection id>` refusal; deleted-source stubs are no longer emitted, and refusals create no skill, resource or signed bundle. MCP retains its unset-identity single-operator read posture and fails closed for a malformed configured identity.

PostgreSQL skill promotion deliberately remains 501 (#3183); no #2804 port is included. Its allowed path is NOT APPLICABLE until that port; regression coverage checks zero mutation in PostgreSQL and the SQLite scratch database on refusal.
