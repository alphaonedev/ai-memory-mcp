Paginate `GET /api/v1/namespaces` (`limit`/`offset`/`prefix`) and cap
`GET /api/v1/stats` `by_namespace` (top-N + `others: {count, namespace_count}`,
or `summary=1` totals only). Both backends now emit the documented
`{namespace,count}` list shape.
