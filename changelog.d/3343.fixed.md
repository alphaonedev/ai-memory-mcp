Paginate `GET /api/v1/namespaces` (`limit`/`offset`/`prefix`) and cap
`GET /api/v1/stats` `by_namespace` (top-N + `others`, or `summary=1` totals
only). Both backends now emit the documented `{namespace,count}` list shape.
