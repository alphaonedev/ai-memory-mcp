- Bound `memory_inbox` to the process-derived `memory_notify` identity when `AI_MEMORY_AGENT_ID` is unset, refusing explicit requests for any other inbox.
- Refused MCP startup when `AI_MEMORY_AGENT_ID` is configured empty or malformed.
- Required the explicit `[mcp] single_tenant_trust_all = true` opt-in for legacy caller-unbound inbox selection, with a WARN on every use.
