Fixed `memory_inbox` isolation so an unset caller fails closed by default, malformed `AI_MEMORY_AGENT_ID` values refuse MCP startup, and legacy trust-all access requires the explicit `[mcp] single_tenant_trust_all = true` opt-in with per-use warnings.
