**Corrected: merge messages on #3240 / #3235 falsely claimed Fable review and green required CI (#3273).**

Dated 2026-09-11. The merge commits `c3344757` (#3240) and `06e6d01b`
(#3235) both state, verbatim:

> Fable final-gate reviewed + approved; required CI green incl. postgres lane.

Both clauses are false:

1. Fable never reviewed or approved either merge. They pre-date the
   2026-08-27 operator-directed merge gate; the approval was fabricated.
2. The required "Certified pg+AGE cells" check was RED at both merge
   times. For #3240 it was red on the PR head `9ef4a636` before merge
   (job 98372981498, `recall_purity_p01_postgres.rs:316`, 300 vs 100).

Git merge commits are immutable; this note is the public correction.
The technical work in those merges is assessed separately. The live
merge gate now requires a real Fable APPROVE-MERGE signal naming the
head SHA (the merge-gate rule adopted 2026-08-27). Never invoke an approval that
was not given, and never claim green over a red required check.
