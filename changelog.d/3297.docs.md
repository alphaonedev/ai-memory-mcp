**#3297 — docs/claims truthfulness (comments + CHANGELOG + operator docs; no product bytes).**

- Stale QUAL-10 `MEASURED` citations re-measured on `1880b1cc` (`wc -l`);
  ceiling constants unchanged (postgres.rs 42_500 / storage 35_200 /
  mcp 17_400 / daemon_runtime 14_550 / config 15_500 / install 3_900).
- Stale docstrings: `SignableLink::valid_from` is caller-supplied after
  #3178 (not "set to now"); `SqliteStore` SAL-scope note records the
  #3173 MCP synthesis owner gate; `MemoryStore::store_batch` default
  docs no longer claim `SqliteStore` does not implement it (#3181).
- #3049 comments/CHANGELOG: inbound signals are queryable, not
  forensic-exported or re-egressed.
- #3197 CHANGELOG: boot fail-open was already closed by #3166; the
  interpolation bug is refused by `config check` before `exec`.
- AGE claim labelling: `find_paths` is the relational recursive-CTE on
  both backends (not AGE Cypher). Re-verified on AGE **1.8.0**:
  `ALL`/`ANY` list predicates now parse; the find_paths
  `ALL(e IN relationships(p) …)` guard still fails at runtime
  (`no relation entry for relid 2`). Capture:
  `docs/kg-find-paths-engine.md`. Product follow-up: #3609.
