#3709 item 2 — `ai-memory tls init | import | status`: the listener's certificate material is operable without reading source. `init` mints the local CA + leaf on purpose (singleton shape only, idempotent); `import` verifies an operator pair exactly as the listener loads it (PEM, key matches leaf, not expired, `--host` covered, `--ca` is the issuer) and installs it under `<key_dir>/tls/` — nothing is written unless every check passes; `status` reports source, issuer, SANs, expiry and the next-boot verdict for the declared shape. The daemon now accepts imported operator material on every shape (the leaf's issuer decides, from the artefact), never mints over it, and refuses an expired one naming the fix; `doctor` reads the same issuer. The three certificate remedies name the verbs; `db check-tls` is still not promised.
