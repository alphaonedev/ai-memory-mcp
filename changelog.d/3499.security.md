CLI/MCP recall and search now apply caller admission followed by optional `as_agent` scope narrowing. A self-asserted scope position cannot reveal private/inbox rows without an enforced caller. HTTP recall/search preserve caller binding; HTTP KG query now honors exact target namespace and scope narrowing on SQLite and PostgreSQL. `memory_list` and `memory_session_start` document that `as_agent` is unsupported.
