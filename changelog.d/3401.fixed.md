Notify and inbox now share the canonical `_inbox/<agent>` namespace, short
notification default tier, and response envelopes on SQLite and PostgreSQL.
Schema v98 aliases legacy live and archived `_messages/<agent>` rows without
rewriting signed data. Unread filtering, quotas, and recipient isolation remain enforced.
