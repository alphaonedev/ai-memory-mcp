Notify and inbox now use one backend-blind contract: new messages land in
`_inbox/<agent>`, omitted tiers resolve to `short`, SQLite and PostgreSQL return
the same receipt and inbox fields, and schema v96 migrates existing live and
archived `_messages/<agent>` rows into the canonical namespace.
