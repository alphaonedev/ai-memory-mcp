`ai-memory logs` now refuses malformed or inverted time bounds, unknown levels/formats, and read-only filters passed to archive/purge instead of silently ignoring them; backup/restore examples and install/wrap help now match the live CLI.
