`ai-memory doctor` now opens local databases strictly read-only, refuses a
missing `--db` without creating or migrating it, and reports signing readiness
for the resolved CLI caller instead of only inspecting the daemon key label.
The SQLite FTS5 deep-integrity command is withheld on that read-only connection
and points operators to the daemon's cached verdict or a maintenance copy.
