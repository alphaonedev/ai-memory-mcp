**#3587 U2 — scaffolded by U5a. Body lands with the Grok unit.**

`ai-memory watch --host file:<path>` as `WatchSource::LineFile` (not `HostKind`); `transcript_line_dedup` idempotency; `refuse_pg_store` + SAL/Postgres path; native fs-notify under `--features fs-notify`. Replace this scaffold when U2 ships.
