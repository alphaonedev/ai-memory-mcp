**Test key-directory fixtures honour the #3198 guard; a gate keeps them honest (#3733).**

Eight integration tests created the directory they then used as the key dir with a bare `create_dir`/`create_dir_all`, so the dir inherited the ambient umask — `0755` at `umask 022`, `0775` (group-writable) at `umask 0002` — and the product's key-directory guard (#3198/#3355) correctly refused it. Two of them flipped red on a `0002` host (`federation_peer_posture_3582`, `cli_stdout_hygiene_behaviour_3436`); six were latent (`authority_boundary_3549`, `boot_posture_banner_2908`, `keys_prune_3355`, `mcp_governance_pre_action_1685`, `mcp_governance_pre_write_hook_1583`, `secret_screen_g29`). All eight now use `tests/common/key_dir_sandbox.rs::mkdir_0700`, and three more arrived with the v1.0.0 merge order — `credential_to_sink_3711`, `migrate_no_create_3435` and `url_allowlist_render_3667` — so ELEVEN fixtures carry the helper on the resolved tree. The product check is unchanged: the verdict was right, the fixtures were wrong. New gate `scripts/check-test-keydir-mode.sh` (CI job `test-keydir-mode-gate` in `c8-precheck`) refuses a test that bare-creates a directory it passes as the key dir; on this branch's own base it names exactly those eight; on the v1.0.0 resolved tree it names eleven, because #3711, #3435 and #3667 each added a key-dir-creating fixture after this branch was cut. A COUNT IS BOUND TO THE TREE IT WAS TAKEN ON.
