Restores the `memory_session_start` tool description to its D1.6-pinned text; #3352's clustering behaviour is unchanged and documented in the API reference, but a tool description is wire-visible and snapshot-pinned, so the explanatory sentence moved out of it.
