Repair the GA acceptance client's pubkey proof-of-possession handshake (it
still sent the pre-v97 flat `PUT /api/v1/agents/{id}/pubkey` and got 403) and
the federation rebroadcast fixture's pre-binding timestamp. Keep Python bind
paths greppable so the SDK route-conformance gate resolves them. The v97
timestamp-eligible key resolver is CONFIRMED correct — a key bound after a
memory's signed `created_at` cannot verify it — so a signed write the receiver
cannot verify no longer vanishes behind a 200: `/sync/push` responses now carry
`attestation_rejections[]` (`{memory_id, cause}`) on both backends, retained
through a later quota refusal, each with a matching cause-specific receiver
WARN. New closed-set cause `no_eligible_key_at_created_at` separates "the
author is enrolled but had no key live at the signed instant" from
"unenrolled_author_strict", whose remedy is the opposite.
