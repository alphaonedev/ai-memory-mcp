AGE outbox drainers retain one immediate bounded boot-recovery pass and redraw a bounded delay after each completed drain, avoiding periodic phase lock and the former duplicate immediate tick. Entropy-failure warnings are limited to once per process; the existing interval, retry bounds and shutdown control remain.
