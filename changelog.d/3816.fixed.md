AGE outbox drainers now spread their first recovery pass and redraw a bounded delay after each completed drain, avoiding synchronized fleet traffic and the former two immediate startup passes. The existing interval, retry bounds and shutdown control remain.
