**`AI_MEMORY_ENCRYPT_AT_REST` is documented as what it is: per-record content encryption on the default build, both backends — not a SQLCipher switch (#3720, ga-blocker).**

`CLAUDE.md` env-table row 37 said the knob was honoured on a "sqlcipher build only" and made every DB open demand a passphrase. Measured on the tree: `encryption::encryption_enabled` has no feature gate; `seal_content` (sqlite `storage::insert`, postgres `insert` / `update` / `sync_push`) writes a `0x03` per-record ChaCha20-Poly1305 envelope keyed under the per-agent X25519 key (ECDH + HKDF-SHA256) into `memories.encrypted_envelope` on both backends; the connection layer's own comment says the knob "is not a SQLCipher request"; `doctor` reports `at_rest = ENCRYPT_AT_REST` at Info on a default build (`encrypt_at_rest_storage_section_agrees_with_serve_boot_b3`). SQLCipher is a separate control keyed by the passphrase channel on a `--features sqlcipher` build. Row 37 now says so, with the key lifecycle (minted on first use under the key dir, no rotation/escrow at v1.0.0, #3717) and the plaintext fields named; `docs/compliance/honest-limitations.md` item 5 names both controls instead of only SQLCipher; `SECURITY.md` names the real config seed (`[encryption] at_rest`). Posture control #15 still reads the knob as a SQLCipher predicate — a T3 ruling, not a doc edit, and named as the open gap. Pinned by `tests/claude_md_env_row_37_at_rest_3720.rs`: the row's Surface cell carries no build condition and its Notes name the mechanism, and a real `ai-memory store` on this default-build binary with the knob set writes an envelope, empties `content`, and mints the x25519 key files.
