Concurrent appends to the PostgreSQL `signed_events` audit chain (and its `memory_revisions` twin) no longer fail the caller's whole write: the losing appender re-derives its `sequence` from a fresh head read instead of surfacing SQLSTATE 23505 on `idx_signed_events_sequence`, while a genuinely replayed event id still fails permanently on the primary key.
