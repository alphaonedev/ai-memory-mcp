**CI: pipefail `printf | grep -q` false-miss in required-context gates (#3608).**

`scripts/check-migration-ladder.sh` rule (e) still used `printf | grep -qx` under `set -o pipefail` after #2414 added `array_contains` for the same footgun: `grep -q` closes the pipe on the first match, `printf` gets EPIPE, and a referenced migration is reported as an ORPHAN. Same class in `check-test-env-lock.sh --self-test`, `check-test-stdin-reads.sh --self-test`, and `check-commit-signing-posture.sh` `is_enrolled_principal`. Membership is now `array_contains` / here-strings (no pipe). The migration-ladder `--self-test` plants a long first-line-match reference list and reproduces the SIGPIPE false-miss.
