Stale quality-family MCP tool docs now match the build (#3397).

`memory_gc` `docs()` claimed the sweep was "SUBSTRATE-WIDE and ungated"
and that the archive path "does not archive link edges" (#3161). Both
were false on this tree: #3204 gates a real sweep (K9 + destructive
delete-governance + forensic allow row; `dry_run` stays ungated) and
#3161 snapshots `memory_links` into `archived_memory_links` so
`memory_archive_restore` re-inserts them. Restore `docs()` was a
one-liner that omitted the edge restore, contradicting the (now true)
gc contract.

`memory_calibrate_confidence.output_format` is still inert (#3171 left
it declared so existing clients are not refused). The field's IGNORED
note is stripped from `tools/list` by `strip_docs_from_tools`, and the
short `description()` is then compacted to 32 bytes
(`compact_description`). The FIRST sentence of `description()` is the
tool purpose (`Calibrate confidence baselines.`, 31 bytes) so an agent
choosing a tool sees what it does; the second sentence is
`output_format is ignored.`; `docs()` keeps the full IGNORED
disclosure (wire-level survival of a trailing caveat is #3378). D1.6
pin path: `memory_archive_restore` and `memory_calibrate_confidence`
added to `DOCS_CORRECTIONS`; `memory_calibrate_confidence` added to
`DESCRIPTION_CORRECTIONS`.
