`migrate` and `sync` now validate the complete final lineage graph before importing links, so valid DAGs round-trip independent of link order or cross-node timestamps while real cycles still fail closed. Migration sources and sync dry-runs open read-only, `migrate --dry-run` never creates its destination, and sync refuses a missing remote instead of creating it and reporting success.
