**`migrate` / `sync` import a valid lineage DAG order-independently and never mint a store they were asked to read (#3435).**
`ai-memory migrate` refused 12 of 289 provenance edges of a real store as a
"reflection cycle": it replayed edges through the per-write Pass-0 wall-clock
lineage guard (#1859), whose single-clock proof does not hold for edges minted
elsewhere, and `sync` swallowed the same refusal silently
(`let _ = db::create_link(..)`). Both now land every node first, write edges
through the inbound funnel that bypasses ONLY that heuristic
(`MemoryStore::apply_remote_link` / `db::create_link_inbound`, which also keeps
the source row's signature and observer verbatim), and assert acyclicity ONCE,
structurally, over the COMPLETE final graph (`storage::lineage_import`) BEFORE
any row is written — a genuine cycle refuses the whole import with a typed
error naming the unresolved nodes, never a dropped edge. `migrate --from` opens
its source through the existing-and-read-only funnel (a missing source is a
typed error, never an empty-store success), `migrate --dry-run` has no
destination in scope at all (`migrate::plan`) so it can no longer create the
`--to` path, `sync` refuses a missing remote instead of creating it and
reporting success, and `sync --dry-run` opens both stores read-only. The
migrate report now also prints the `links_read` / `links_written` /
`links_skipped` tallies the docs already described.
