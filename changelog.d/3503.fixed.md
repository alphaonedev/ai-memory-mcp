CI now runs the full test suite for identity, binding, visibility, federation,
storage, migration and C8 bypass-allowlist changes, including acceptance targets
declared in Cargo.toml. The safety check spans the whole PR so incremental follow-up pushes
cannot omit its security changes; docs-only changes still skip Rust tests.
Committed-diff selector and workflow regressions run in CI.
