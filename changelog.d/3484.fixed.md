- **[storage] embed-skip derived-cache writes (#3344) are record-stop gated on both backends** ([#3484](https://github.com/alphaonedev/ai-memory-mcp/issues/3484)). `record_sqlite` / `invalidate_stale_sqlite` and the Postgres scan call sites refuse (best-effort: skip) under an engaged record stop; the b7 structural scanner no longer attributes const-held write SQL to the preceding helper (const-reference tracking is #3485).
