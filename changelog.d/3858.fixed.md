`examples/fed_issue.rs`'s nine tests — including the #3783 0600-credential regression pin — now run under plain `cargo test` on every CI posture (`[[example]] name = "fed_issue"` with `test = true`). Examples default to `test = false`, so those tests had never been executed by CI, by the landing driver, or by any clippy form; they were runnable all along (9/9 under the default posture without `sal`, 9/9 under `sal`, no temp state).
