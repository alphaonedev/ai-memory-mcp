Refuse contradictory MCP parameter shapes on the coordination read surfaces
instead of failing open: `memory_signal_inbox` now requires `namespace` and
rejects a non-string `to_agent` (which previously dropped the recipient filter
and exposed other agents' direct signals), `memory_action_list` rejects a
non-string `namespace` or an unknown/non-string `state` (which previously
returned `done`/`failed` rows to a caller polling for `pending` work), and
`memory_action_get`, `memory_action_edges`, `memory_signal_read` and
`memory_checkpoint_verify` refuse a missing/blank/non-string `id` instead of
answering with a plausible empty success.
