Identity and checkpoint unit-test key directories now use an explicit mode-0700 fixture, and a targeted SAL CI gate runs those suites under `umask 002` so production permission checks remain strict without making tests host-umask-dependent.
