`memory_pending_approve` / `memory_pending_reject` and
`POST /api/v1/approvals/{id}` refuse `remember='forever'` (#3394).

The previous contract accepted `forever` and returned success while
recording only a process-local `SYNTHETIC_RULES` entry — lost on every
MCP stdio exit, and never consulted by `enforce_governance`. That was
a false success. Honourable values are `once` (default, this row only)
and `session` (this process). Durable persistence is #3580 (v1.1.0).

Tool `description()` / `docs()` and the live operator docs
(`API_REFERENCE`, `ADMIN_GUIDE`, `k10-sse-approvals`) state the
contract; `tools/list` snapshots are re-blessed under D1.6.
