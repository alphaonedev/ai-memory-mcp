**Fixed: harness predicates are fail-closed (#3543).**

Five published-figure oracles could not fail for the defect they claimed
to detect. Big-10 plaintext now requires a TLS-layer curl exit in
{35,52,56} (a plaintext 401 is still a listener); anonymous write
requires 401/403 + a documented error `code` + zero memories delta.
Continuity readiness is a recall hit on the seeded row (not
`embedder_ready`); retention compares payload digest + version, not
GET-by-id 200; the published clock is
`clock_1_harness_restart_to_health_ok_ms`. Stored `attest_level`
mismatch is a non-zero exit (201 is not proof); the inert XDG
`tier=keyword` override is removed. Swarm `covered` requires a
persisted `memory_id` or a documented EXPECTED_REFUSAL; `pending` is
its own bucket. Five negative fixtures are red under the legacy
oracle and green-as-FAIL under the new one.
`sdk/python/swarm/tests` is on the pytest `testpaths` so CI runs it
(#3447).
