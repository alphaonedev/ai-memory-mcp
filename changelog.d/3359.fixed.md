`memory_routine_run` now applies the direct-action validation, secret-screen, and storage-quota controls to every materialized action, caps serialized arguments at 64 KiB, and rejects unknown template edge types instead of coercing them to siblings.
