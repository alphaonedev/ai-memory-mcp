**CI: retire the standalone iOS xcframework re-publisher (#3611).**

`.github/workflows/mobile-ios-republish.yml` was a second `workflow_dispatch` publisher that checked out a tag by name and attached an xcframework with no annotated-tag, signature, or CI-qualification check. `release.yml`'s `mobile-ios` job (OIDC-bound provenance, after preflight) is the only iOS publisher. Historical review-evidence CSV rows that name the deleted file are left untouched.
