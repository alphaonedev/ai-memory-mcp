### Fixed

- **#3553 (data-integrity) — `doctor` and `doctor --posture` attest the SQLite
  `PRAGMA synchronous` level and its durability class.**
  ([#3553](https://github.com/alphaonedev/ai-memory-mcp/issues/3553)).
  Plain `doctor` reports `synchronous` (live, on its own read-only
  connection), `synchronous_resolved`, `durability_class` (`local-only` +
  fsync cadence) and `rpo_on_power_loss`, CRITICAL when a hardened /
  certified posture runs below `FULL`; the enterprise-federation posture
  gains check #21 (`FULL`/`EXTRA` passes, else FAILS naming `synchronous`).
  `open_read_only` now mirrors the resolved level, closing the false green
  where a read-only connection answered SQLite's compiled `FULL` on a
  `NORMAL` process. `DEFAULT_DB_SYNCHRONOUS` stays `NORMAL`. Durability
  table: PERFORMANCE.md §"Power-loss durability".
