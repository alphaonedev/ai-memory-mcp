Fix the cloud-init ASCII gate on macOS by replacing GNU-only PCRE with a portable byte-range scan, propagating scanner errors, and keeping self-test fixtures and scratch under `.local-runs/`. Self-tests cover a copied template, an otherwise-empty template, and UTF-8.
