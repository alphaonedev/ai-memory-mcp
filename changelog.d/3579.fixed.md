Preserve the independently resolved HTTP caller on SQLite notifications.
The stored sender, quota attribution, receipt and wake event no longer pass
through the MCP client-name or ambient daemon identity ladder. PostgreSQL
attribution and the MCP host identity resolution remain unchanged.

The shared SQLite notify helpers now carry `MemoryError` internally while
preserving MCP error messages and the opaque HTTP refusal envelope. The
QUAL-6 legacy signature ceiling remains 132 (2026-09-10 ruling).
