Fix #3811 and #3860: resolve LLM and auto-tag backend default URLs through the
canonical alias table, so `vllm` uses `http://localhost:8000/v1` instead of
Ollama's port. Unknown backends and generic `openai-compatible` have no default
URL. The doctor LLM probe refuses unrecognized selectors before sending a
request or credential, even with an explicit URL. Recognized explicit URLs
and the Ollama default retain their existing precedence.

GA must-ship authority: #3824, five-agent vote `4d3ea1c5`.
