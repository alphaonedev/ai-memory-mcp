Reserve `a2a-hub` and all `a2a-hub/...` scoped forms as wire agent identities.
HTTP header/body identity resolution and MCP explicit caller resolution refuse
the hub delegation domain; ordinary agent-name grammar is unchanged (#3578).

Add the wake-hub zero-authority qualification gate: exact reviewed crate
symbols, forbidden database/key configuration, and mutation-tested scanning
through production code after inline tests. Pin the shared public-key decoder
exception without admitting other keypair APIs.

Pin the binary wake hint with shared Rust/Python/TypeScript acceptance vectors:
five metadata fields, the exact 256-byte boundary, appended content/title and
oversize refusal, truncation refusal, and reserved body kinds 11/12/13.
