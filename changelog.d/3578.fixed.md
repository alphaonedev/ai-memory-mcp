Reserve `a2a-hub` and all `a2a-hub/...` scoped forms as wire agent identities.
HTTP header/body identity resolution and MCP explicit caller resolution refuse
the hub delegation domain; ordinary agent-name grammar is unchanged (#3578).

Add the wake-hub zero-authority qualification gate: exact reviewed crate
symbols, forbidden database/key configuration, and mutation-tested scanning
through production code after inline tests. Pin the shared public-key decoder
exception without admitting other keypair APIs.

Pin the binary wake hint with shared Rust/Python/TypeScript acceptance vectors:
five metadata fields, the exact 256-byte boundary, appended content/title and
oversize refusal, truncation refusal, and reserved body kinds 11/12/13.

Pin the existing 12-key wake-listen JSON output envelope and socket-level
binary refusal before a renderable wake event. Preserve reporting fields;
the 256-byte bound remains on encoded binary metadata, not JSON output.

Pin hub admission versus content-plane credentials on SQLite and live
PostgreSQL: API-key refusal, hub-domain/delegated-key write-signature refusal,
MCP local and forwarded write controls, and unchanged memory rows on denial.
Keep host, transport and process-isolation evidence distinct.

Add the forward-binding content-plane source allowlist over producer and
consumer paths. Pin caller-scoped catch-up reads, metadata-only hook fields,
and the explicitly admitted audited operator spawn. Mutation tests refuse
automatic decoder-to-content-write changes. Document SDK callbacks and hooks
as external operator boundaries; a future content decoder needs a reviewed
SDK-edge screen and explicit allowance.
