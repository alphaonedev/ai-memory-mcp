HTTP GET-by-id on postgres uses the per-anchor `get_links_for_anchor`
SAL surface instead of scanning the entire `memory_links` table via
`list_links(None)` on every GET (sqlite GET already used `db::get_links`).
