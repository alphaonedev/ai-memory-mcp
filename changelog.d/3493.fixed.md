PostgreSQL bootstrap now leaves the v96 embed-skip function and trigger to the
v96 migration instead of replaying an ACCESS-EXCLUSIVE drop/create pair before
the ladder. Ordinary readers can no longer leak a raw bootstrap lock-timeout,
and pre-v96 table shapes no longer see a trigger that names absent columns.
