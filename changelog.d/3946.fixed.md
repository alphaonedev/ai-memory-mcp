PostgreSQL rewind and contamination stamps now walk committed relational lineage even when AGE projection is pending. Rewind cost accounting uses the same selected nodes; general lineage query dispatch and caller authority are unchanged.
