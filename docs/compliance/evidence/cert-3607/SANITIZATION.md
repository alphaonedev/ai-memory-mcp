<!-- Copyright 2026 AlphaOne LLC / SPDX-License-Identifier: Apache-2.0 -->

# cert-3607 evidence sanitization (2026-09-11 re-issue after #3549 / #3553 / #3199)

These files are **recorded artifacts** of the enterprise-federation
certification re-issue run on 2026-09-11 against the published release tip
`b048311595caa94db435c3dd9b59b02ea4b765f2` (`origin/release/v1.0.0`). They
are **not** regenerated by CI. Host: a Linux build host; the certified
PostgreSQL 18 + AGE + pgvector tier over TLS (`sslmode=verify-full`); an
exclusive throwaway live database created for this run (never an operator
or shared database); release-built binaries of the same tip (one default,
one `--features sqlcipher`) for the four §5.4(2) posture legs.

## The artifacts

| File | What it records |
|---|---|
| `test-results.txt` | Exact `test result:` lines from each cargo invocation (default features, then `--features sal,sal-postgres`; the §7 federation suites, the doctor posture suites incl. #3553, and the lib slices `enterprise_federation_posture`, `federation::peer_posture`, `cli::backup::tests` for #3199). |
| `named-tests.txt` | Exact `test <name> ... ok` lines for those suites (sqlite + postgres twins). |
| `posture-bare-env.out` | Bare env (`AI_MEMORY_NO_CONFIG=1`, nothing set): exit 2, **10 `[FAIL]` / 12 `[PASS]` of 22**. |
| `posture-hardened-env.out` | Hardened env WITHOUT sqlcipher and WITHOUT the boot gate armed: exit 2, **2 `[FAIL]` / 20 `[PASS]`** (`AI_MEMORY_ENCRYPT_AT_REST`, `AI_MEMORY_REQUIRE_ENTERPRISE_FEDERATION_POSTURE`). |
| `posture-hardened-boot-refusal.out` | The SAME hardened non-sqlcipher env WITH `AI_MEMORY_REQUIRE_ENTERPRISE_FEDERATION_POSTURE=1`: the binary **refuses to boot** (exit 1), naming the below-floor control. |
| `posture-sqlcipher-pass.out` | The certified configuration (`--features sqlcipher`, `AI_MEMORY_ENCRYPT_AT_REST=1`, boot gate ARMED, `AI_MEMORY_DB_SYNCHRONOUS=FULL`, operator key pair in the key dir): exit 0, **22 `[PASS]` / 0 `[FAIL]`**; check #21 observes the live `PRAGMA synchronous` on an encrypted fixture store; check #22 finds the operator key. |
| `posture-legs-exit-codes.txt` | The four recorded shell exit statuses (2 / 2 / 1 / 0). |
| `peer-fingerprints.txt`, `peer-attestation.json` | The throwaway fixture peer map used by the hardened legs (placeholder fingerprint, placeholder peer). |
| `MANIFEST.sha256` | sha256 of every file above. |

## What was sanitized / omitted

- Cargo progress bars, compiler warnings, and absolute `CARGO_TARGET_DIR`
  paths were **not** copied. Only the `test result:` / `test <name>` lines.
- Absolute checkout, fixture and home paths in the posture captures are
  collapsed to `<repo-root>` / `<home>`.
- The live DSN (including the postgres role password), the fixture
  database passphrase and the fixture key material were process-env /
  fixture-file only and do **not** appear in these files.
- No API keys, passphrases, PEM blocks, peer IDs from production, internal
  host names, ports or shared database names.

## What was NOT present (and therefore not redacted)

- No `skip:` lines and no `ignored; N` with N>0 in the named suites.
- No `test result: FAILED` lines.
