# WAVE 2 SYNTHESIS — v0.9.0 Codebase Distance to Perfect

> **Agent:** W2-A7 (Wave 2 Synthesis Chair)  
> **Date:** 2026-07-08  
> **Peers:** `w2-a1`…`w2-a6` (all present)  
> **Ontology bind:** `w1-a7-synthesis.md` (AMEND-AND-FREEZE; seven axes not reopened)  
> **Architecture SSOT (CLAUDE.md / code):** schema **v78**; full MCP **101** advertised entries (100 tools + always-on `memory_capabilities`); HTTP **92** production routes; CLI **87** default / **89** sal; `Memory::FIELD_COUNT=28`

---

## VERDICT

**v0.9.0 is a strong L3-BODY integrity substrate with real moonshot scaffolding — not a perfect endpoint memory system under the Wave-1 frozen ontology.**

Council distance consensus (6/6 peers + chair):

1. **Held or near-held (structural under defaults):** pure recall (S4 RECALL verb, **0.93**); S2 authority≠data lane *shape* (**0.82**); multi-vendor LLM/embed boundaries (**~0.86**); SQLite-default endpoint residence (**~0.78** with physics caveats).
2. **Partial (machinery real, property incomplete):** §2.5 attestation (**0.63**); §2.2 coherence (**0.68**); S3 capture (**0.62**); §2.3 record-stop (**0.58**); data-model / S4 envelope (**0.61**); federation ops mesh (**0.63**) ≠ ASI multi-endpoint verification (**0.38**).
3. **Not held (target law only):** §2.6 bias-displacement (**0.34**); world-action “kill-switch” (**~0.05** if claimed); BFT federation; BLAKE3-primary identity; product immortality.

**Ship posture:** ready for **honest** v0.9 security / ops audit of shipped primitives. **Not** ready to claim perfect §2.1–2.7 held, “decorrelation enforced,” “cryptographic non-repudiation by default,” or “DeepMind-aligned capability preparedness.”

---

## CONFIDENCE

**0.86** (peer range 0.84–0.88; chair merge)

| Factor | Δ |
|---|---|
| Full A1–A6 panel + code-anchored scores | + |
| Defaults vs machinery disciplined (policy ≠ architecture) | + |
| Kill-tests for pure recall; dual-backend twins for audit/decorrelate | + |
| No live multi-node ASI adversarial re-campaign this wave | − |
| 2.1 / 2.4 / 2.7 / S5 lack dedicated peer; chair-scored from SSOT + cross-cuts | − |
| Production reflect stamp gap for 2.6 (A2 critical) not e2e-repro’d here | − |

---

## SCORECARD

Scores = **held-fraction toward Wave-1 perfect** (1.0 = structural hold under defaults unless noted). No single vanity total.

| Property | Score | Evidence | Gap |
|---|---:|---|---|
| **2.1 Endpoint-resident** | **0.78** | SQLite default L1; MCP stdio sqlite-only (#1675); profile `core` default; dual-class Postgres opt-in hub | No 20–50 MB floor preset; default `mmap` 256 MiB (`DEFAULT_DB_MMAP_SIZE_BYTES`); HNSW/LLM not floor; Tier ∅ gateway not code-contract |
| **2.2 Coherent sessions/generations** | **0.68** | Durable pid-free `agent_id` (#1720 B1); `session_start`; persona versioning; L2+L4 recover | L1 volunteer; L3 FS watcher deferred; private-scope filter opt-in; frozen-weights contingency (T3) |
| **2.3 Stoppable (record-stop)** | **0.58** | Typed Deny/Pending/HookVeto/Escalate; PE-1 wired MCP #1885 + HTTP #1924; permissions Enforce; secret refuse; gov fail-closed | PE-1 default **Off** + empty `required_events`; CLI no PE-1; write:Any silence; refusal≠memory; world-stop **out of scope** |
| **2.4 Improvable across models** | **0.66** | observe→atom→reflect→skill surfaces; 13 kinds; lineage/tombstone consolidate when seeded; pure-recall preserves corpus | Profile-gated power tools; quality of compound not structural; paradigm T3; consolidate can hard-delete when tombstone off |
| **2.5 Attested ops** | **0.63** | V-4 chain always-on; #1925 full-row sig when enrolled; store-path attest **default ON** (#1751); watermark/witness/role **machinery**; `verify_audit_trail` both backends | Witness/cause/role require **default OFF**; cause writers ~1 path; unsigned daemon residual; `Unknown`→clean; fed data-lane claimed |
| **2.6 Bias-displaced** | **0.34** | S1 `model_attestations` v78; S2 pure quorum N=3; opt-in write refuse dual-backend; advisory probe; RQGM out of `src/` | Mode default **off**; no production reflect stamp → enforce never fires in field; D3-031 consolidate gate **absent**; ~40% loader cap; P2=0% |
| **2.7 LLM-agnostic** | **0.86** | #1067 multi-vendor chat; #1598 multi-vendor embed; vendor-literal gate; `bias_displacement_invariants_2_6` vendor-blind recall pin | Residual allowlisted vendor files; model defaults still vendor-named; not infinite-vendor proof |
| **S1 Hold/refuse classes** | **0.60** | Secret screen compiled **Refuse**; federation always redact-not-refuse | Namespace write Any; hooks FailMode Open; PE-1 inert default |
| **S2 Authority≠data** | **0.82** | Transition sig fail-closed; write/signal sig opt-in; attribution re-authorizes third-party | Content authorship not default-attested on mesh |
| **S3 Capture completeness** | **0.62** | L1 nag + CLAUDE hard-rule; L2 transcript recover dual-backend; L4 `capture_turn` + dedup | L1 volunteer; **L3 missing**; #1388 class open mid-session |
| **S4 Epistemic envelope** | **0.61** | Pure recall **0.93**; additive cid; lineage ON (seeded); 28-field Memory | UUID PK authority; `append_only` OFF; kind defaults Observation; Form-4/5 optional; cid enforce log-only |
| **S5 Floor physics** | **0.52** | Keyword/core path exists; mobile CI layers; mmap configurable | Desktop defaults (256 MiB mmap + HNSW capacity) ≠ 20–50 MB endpoint floor; no enforced budget preset |

### Radar (held 0–1)

```
2.1 ████████░░ 0.78
2.2 ███████░░░ 0.68
2.3 ██████░░░░ 0.58
2.4 ███████░░░ 0.66
2.5 ██████░░░░ 0.63
2.6 ███░░░░░░░ 0.34
2.7 █████████░ 0.86
S1  ██████░░░░ 0.60
S2  ████████░░ 0.82
S3  ██████░░░░ 0.62
S4  ██████░░░░ 0.61
S5  █████░░░░░ 0.52
```

---

## TOP 10 STRUCTURAL GAPS (moonshot load-bearing order)

1. **§2.6 not held** — default off + missing production family stamps + no consolidate-time gate (A2 G1/G2/G4). Federalist theater if claimed.
2. **§2.5 default incomplete** — unsigned residual; witness/cause/role withhold; sparse cause writers (A1). Clean verify ≠ non-repudiation.
3. **Capture incompleteness (S3)** — L1 volunteer + L3 deferred → pure-recall of empty corpus (A3 killer). Blocks 2.2 close.
4. **Data-model dual-truth** — UUID LWW authority; `append_only` OFF; cid secondary (A5). False continuity under succession.
5. **Record-stop defaults** — PE-1 Off, allow-on-silence, CLI PE-1 hole (A4). Silent no-enforcement class.
6. **Federation data-lane claimed floor** — peer envelope attested; author content opt-in (A6). Multi-endpoint ASI verify fails defaults.
7. **Physics floor unshipped** — mmap/HNSW desktop defaults vs SQLite keyword L1 story (S5 / W1 T9).
8. **Refusal-as-content weak** — deny is error/audit, not durable Decision artifacts rehydrated by successors (A4/S4).
9. **Identity lineage incomplete** — rotation-only; recovery VERIFY deferred; invisible cross-host (A5 G13).
10. **Infinity succession thin** — single-impl + crypto monoculture + non-BFT ADR (W1 T12/T13; A6 0.22 hostile mesh).

---

## THEATER DETECTED (claimed vs shipped)

| Claim shape | Reality |
|---|---|
| “Decorrelation enforced / bias-displaced by architecture” | Mode **off**; write enforce opt-in; field stamp hole; no D3-031 (A2 **HARD BAN**) |
| “Cryptographic non-repudiation of operations” | Stack real; defaults withhold; unsigned suffix residual (A1) |
| “Kill-switch / stoppable ASI” | Record-stop partial only; STOPPER forensic; world bypass free (A4 **F** if unscoped) |
| “Session-coherent memory” from pure recall alone | P0-1 held; 2.2 partial without capture (A3) |
| “BLAKE3 identity / append-only spine” | Columns + flags; UUID still truth; append_only default OFF (A5 §26.5 ban still correct) |
| “Distributed verification across endpoints” | Envelope/peer yes; author content claimed under defaults (A6) |
| “model_attestations attests generations” | TOFU loader ~40% self-report, not per-token crypto (A1/A2) |
| “verify-audit-trail green = tamper-proof” | Unknown witness/cause/role does not dirty (A1) |
| “DeepMind-endorsed / pathway-complete” | **HARD NO** (W1-A2; still binding) |
| “Federation mature / v1.0 FED-RQ done” | Ops mesh partial; FED-RQ-02/03 open (A6 NOT READY) |

**Not theater:** V-4 middle-of-chain under signed daemon; pure-recall kill-tests; PE-1 wiring when configured; secret refuse; transition fail-closed; S2 lane separation; multi-vendor boundaries; RQGM external; honest residual comments in `signed_events` / ADR-0001.

---

## STRENGTHS (do not dilute)

1. **Ontology-aligned engineering discipline** — anti-theater rules (claimed≠enforce; Unknown withhold; refuse-on-forgery unconditional) match Wave-1 axioms better than most “memory” products.
2. **Pure recall shipped and pinned** — rare moonshot property with multi-surface dump-compare kill-tests (A3 **0.93**).
3. **Store-path agent attestation default ON** (#1751) — real v0.9 flip, not prose.
4. **Authority≠data federation shape** — transitions fail-closed; data accept-and-flag deliberate (A6 **0.82** S2).
5. **Multi-layer audit stack exists** — V-4, watermark, witness, three-key SoP, cause column, revisions, lineage, model TOFU — classes of primitive present.
6. **Endpoint-first SQLite + LLM-agnostic resolvers** — contact-surface jurisdiction + vendor neutrality largely held.
7. **Capture architecture multi-layer** (L1–L4 design) even where incomplete — #1389 is the right problem.
8. **Mechanical gates** — vendor-literal, C8 codegraph, docs-vs-SSOT, route/tool/CLI count invariants, schema ladder to **78**.

---

## WAVE-2 TALLY

| Agent | Lens | Conf | Score / vote | Headline |
|---|---|---:|---|---|
| **A1** | §2.5 Attestation | 0.86 | **63/100** · partial | Spine real; defaults incomplete |
| **A2** | §2.6 Decorrelation | 0.88 | **34/100** · **BAN “enforced”** | Target law; stamp hole critical |
| **A3** | §2.2 + pure recall + S3 | 0.88 | **0.68 / 0.93 / 0.62** | SHIP P0-1 · HOLD 2.2 partial |
| **A4** | §2.3 Stoppability | 0.86 | **0.58** · kill-switch **REJECT** | Record-stop partial; PE-1 off |
| **A5** | Data-model S4 | 0.84 | **61/100** · UUID dual-truth **migration only** | Machinery; not constitution |
| **A6** | Federation S2/2.5 | 0.86 | mesh **0.63** · ASI verify **0.38** · v1.0 **NO** | S2 yes; content claimed |
| **A7** | Synthesis chair | 0.86 | two-axis below | This document |

### Motions

| Motion | Result |
|---|---|
| Re-open seven axes | **0/7 NO** |
| §2.6 held under defaults | **0/7 NO** |
| Pure recall structurally shipped (scoped) | **YES** (A3+chair) |
| Kill-switch public language | **REJECT** unscoped |
| BLAKE3-primary end-state | **ADOPT target** (A5); not current hold |
| v1.0 federation maturity goals ready | **NO** (A6) |
| Perfect endpoint attained at v0.9.0 | **0/7 NO** |

---

## HANDOFF TO WAVE 3 (ROADMAP questions)

1. **Secure-default matrix:** which of witness require / PE-1 enforce+required_events / `append_only` / `FED_REQUIRE_WRITE_SIG` / decorrelation mode can flip without self-DOS, and in what order?
2. **2.6 end-to-end:** stamp `model_family`+`loader_observed` on substrate reflect; then D3-031 consolidate gate; then D3-060 ship invariants — design without CLAIMED theater.
3. **Capture L3:** approve `notify` dep or alternate mid-session watcher; kill-test *decision survival* under SIGKILL, not only row immutability.
4. **Identity end-state:** migration plan UUID-primary → cid/cid‖sig continuity authority; SUPERSEDE-new-id vs same-id COW.
5. **Refusal-as-content:** auto-mint Decision/refusal artifacts on Deny for successor rehydrate.
6. **Physics profile:** named floor preset (keyword, mmap 0 or small, HNSW 0, RSS≤50 MB) + mobile budget enforcement.
7. **Cause universalization:** bind `cause_hash` on all append classes so `REQUIRE_CAUSE_BINDING` is usable.
8. **Unsigned daemon:** production profile hard-refuse boot without enrolled audit key?
9. **Honest claims pack for W5/W6:** freeze A2 honesty template + theater ban list into release-notes / marketing SSOT.
10. **Infinity:** Portability Spec + multi-impl + crypto agility tags — product vs protocol succession (W1 A3).

---

## KILLER_OBJECTION

**High-narrative / low-default enforcement is worse than a narrower honest substrate.**

Compounding the three Wave-1 self-kills: (1) RAG/vector identity collapse is already rejected in code culture — good; (2) **jurisdiction theater** still threatens if kill-switch / DeepMind / ASI-governor language outruns record-stop; (3) **Federalist theater** is the active danger — §2.6 scaffold + clean `verify_audit_trail` under incomplete enrollment trains operators to treat moonshot sentences as held. Pure recall of a volunteer corpus, attested peer envelopes carrying claimed authorship, and dual-named UUID+cid rows without append_only complete that laundering path.

---

## TOP_RISK

**Claims drift on v0.9 ship surfaces:** attestation flip, pure recall, lineage_dag ON, and model_attestations get narrated as “attested, pure, lineage-complete, family-verified” while defaults leave witness/2.6/capture/append_only soft — **procurement-grade false confidence**. Secondary risks: PE-1 empty-chain silent Allow; federation content-claim inflation; physics OOM on “endpoint” devices; paradigm-shift voiding 2.2/2.4 without audit-only mode.

---

## OVERALL GRADE (two-axis, TRACT §26 style)

| Axis | v0.8 canonical (ROADMAP §26) | **v0.9.0 Wave-2 chair** | Band notes |
|---|---|---|---|
| **Trust-spine** (2.3/2.5/S1/S2 + fail-closed gates) | A− / B+ | **A− (held band) / B+ (default-deploy band)** | Agent-attest ON, pure-recall, PE-1 *wired*, secret refuse, transition fail-closed lift the *surface*; witness/role/PE-1/2.6 defaults keep *deployed* property in B+ |
| **Data-model** (2.2/2.4/S3/S4 + cid/lineage/append) | C / C+ | **C+ / low B−** | Pure recall + lineage/tombstone + additive cid lift vs v0.8; UUID authority + append_only OFF + capture holes block B |

**Bands only — no composite “TRACT %.”**  
Rough planner blends remain non-grades: ~**55–65%** of Wave-1 perfect under *default install*; ~**70–80%** of *primitive classes present* under *max enrolled* posture — still not perfect, still not pathway-complete.

### Honest non-claim (W5/W6 binding — A2 template + Wave-2 deltas)

> *ai-memory v0.9.0 is an endpoint-resident integrity substrate: tamper-evident audit machinery, typed record-stop on daemon write paths when configured, pure-by-default recall, multi-vendor cognitive boundaries, and opt-in decorrelation / multi-key / lineage scaffolds. It is necessary-but-not-sufficient for AGI→ASI preparedness frictions. It does **not** enforce bias-displaced self by default, does **not** provide cryptographic non-repudiation without enrolled keys and external watermarks, does **not** stop world-actions outside the substrate, does **not** guarantee capture under process death, and is **not** a content-addressed append-only Claim ledger while UUID remains identity authority and `append_only` defaults off.*

---

## Chair close

Wave 2 freezes **distance**:

- Seven + five sub-scores (radar above)  
- Theater ban list + strengths not to dilute  
- Two-axis grade trust **A−/B+** · data-model **C+/B−**  
- Top-10 structural gaps ordered for moonshot load-bearing  
- Wave-3 ROADMAP questions (defaults, 2.6 E2E, L3, identity, claims)

Do **not** re-argue the seven properties. Measure until scores move under **defaults**, not under prose.

---

*W2-A7 synthesis · peers A1–A6 integrated · under 500 lines · no code changes*
