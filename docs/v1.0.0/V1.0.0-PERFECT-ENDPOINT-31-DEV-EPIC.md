# v1.0.0 Perfect-Endpoint — 31-Item Development Epic (AI-NHI autonomous)

**Operator directive 2026-07-13 (grant `898a36af`; roadblocks-by-2×5-vote `64ceadef`; memory `1c19431c`).**
v1.0.0 scope is **expanded from the near-perfect converged 27-axis substrate to the FULL
perfect / definitive endpoint.** The prior SHIP-RECOMMENDED (`4d9a7368`) is **superseded** —
**v1.0.0 does NOT GA until all 31 items below are 100% complete** *and* the entire Gate-3
full-spectrum re-runs clean. AI-NHI is 100% autonomous. The tag cut, manifest bumps, and
`release.yml` remain operator-gated.

## Ground truth / sources
- Perfect-endpoint spec: `docs/reviews/PERFECT-ENDPOINT-MEMORY-SPEC-3x7-FABLE.md` (27 axes + §5 future-candidates + §7 amendments).
- Definitive endpoint: `docs/design/TRACT-the-definitive-endpoint-ai-memory.md` (TRACT-gaps G##).
- Program spine: `ROADMAP.md` §27; env table + architecture in `CLAUDE.md`.

## The 31 (by tier; each is a 1:1 GitHub issue, milestone v1.0)

### Tier A — converged-spec residue (highest priority; closes the "honest limits")
- **#1968** R11 — federation E2E scoped-ciphertext content encryption (a peer structurally cannot receive plaintext/keys outside its grant).
- **#2002** FED-RQ-02 — equivocation detection/eviction runtime + epoch-manifest federation.
- **#2004** R75 — crypto-agility operational runtime: re-anchor ceremony + un-freeze/PQ path.
- **#2006** R72 — integrity-complete JSON exporter: emit+import round-trip of the full signed spine.
- **#1950** read-path consumer-binding envelope (cid-anchored, signed-events-folded).
- **#1852** mesh-wide un-forget: signed propagating tombstone-revocation primitive.
- **#1837** R24 residue — additive CC0 conformance vectors (core discharged v80; add revocation/recovery-quorum/multi-record-chain vectors).
- **#1978** A-CAP — L3 substrate watcher (filesystem-notify capture daemon) + process-death kill-test.
- **#1977** C-ABI FFI — ship the real `no_mangle` extern-C surface (A13 drop-in).
- **#1980** PE-7 — refuse-by-default signed rule pack (**proof/template only**; apply/enforce v1.x [#2047](https://github.com/alphaonedev/ai-memory-mcp/issues/2047)).
- **#1873** S5 residual — audit-trail governance-erasure completeness (#1850 tail).

### Tier B — TRACT definitive-endpoint gaps (data-model / longevity ceiling)
- **#1836** G22 — six-verb Claim algebra (vs thick 27-field row / RPC verbs).
- **#1833** G19 — open-predicate-over-kernel (vs closed 9-relation enum).
- **#1834** G20 — claim-level bitemporal algebra (columns shipped v79; add the operators).
- **#1832** G18 — human↔AI covenant clauses (why_trace, immutable authorship, permanent dissent, signed forget receipt).
- **#1830** G16 — (n,k) erasure-coded no-primary cold tier.
- **#1829** G15 — Landauer cost-of-access gradient (vs flat TTL-tiers).
- **#1839** G31 — latency-SLO degrade actuator (empty/A/B/C degradation manifest).
- **#1862** G10.2 — refusal-as-recallable-Claim.
- **#1863** G10.3 — promotion-court replacing access-count auto-promote.
- **#1864** G10.4 — bridge-capability for cross-namespace recall (namespace as enforced isolation boundary).

### Tier C — features + performance
- **#1860** persistent vector index substrate (vectorlite) — deferred from #1005.
- **#1868** streaming tool responses for long-running MCP tools.
- **#1974** partial content edits (patch primitive) for `memory_update`.
- **#1390** direct-API SDK shims (Anthropic + OpenAI; Python + TypeScript).
- **#1979** Claude Code plugin marketplace manifest (`.claude-plugin/`).
- **#1988** wire the 8 unshipped v0.8.0 coordination hook events.
- **#1707** live recall-utility term (feedback-driven rerank; gated on shadow validation).
- **#1969** reranker global default-on flip (per the #1867 vote's shadow condition).
- **#2024** operator-authorized lifecycle management for registered skills.

### Tier D — engineering quality / tech-debt / flakes
- **#1802** refactor: `storage/mod.rs` split + `MemoryStore` trait decomposition.
- **#1875** full curator unification (RQ-PARITY-02: port the six conn-bound extras).
- **#1876** filter-offset / probe paging past `LIST_MAX_LIMIT`.
- **#1877** SAL `list()` contract asymmetry (PostgresStore limit clamp).
- **#1803** IronClaw lan-parity compose peer-key auto-provisioning.
- **#1987** wire `bench --baseline` regression guard into CI.
- **#1989** intermittent full-suite wedge flake · **#1998** config env-var race · **#1994** serve_integration Windows race · **#1993** Code-Coverage job timeout · **#2000** HTTP signed-bulk EMIT test-coverage.

## Development process (every item)
1. **Recall** relevant memory + read the issue + its spec/ROADMAP anchor.
2. **Codegraph-anchored** understanding (`codegraph_explore`; re-index at program start and before each review round).
3. **Load `/rust-skills` + `/rust-microsoft`**; cite `M-*`/rule IDs on all `src/` work. No hardcoded literals (the `c8`/vendor/literal gates).
4. **2×5 adversarial vote at every crossroads** (`Tn` triggers — public-contract/format/security/hard-to-reverse/spec-deviation/≥2-paths). Cite `4d3ea1c5`.
5. **Implement** on a feature branch; **1:1 issue discipline** for any new finding.
6. **Test**: unit + integration + sqlite↔postgres parity (`--include-ignored` for `sal-postgres`); NEVER `cargo test --lib --tests` (ENOSPC) — targeted `--test`. Add regression tests; NEVER lower coverage floors (add tests).
7. **Gates**: `cargo fmt` + `clippy -D pedantic` + targeted tests + `cargo audit` + the docs-vs-SSOT + literal/vendor gates.
8. **PR** (base main) → merge on green (3× `Check(os)` + Per-Module Coverage). Close the issue with retest evidence.

## Whole-program completion gate (Gate-3 FULL re-run — only after all 31 close)
1. **Codegraph reindex** of final code.
2. **Full-spectrum testing** (functional / federation W-of-N / migration round-trip / chaos convergence).
3. **Multi-agent code review** (correctness/reuse/efficiency/altitude + Rust-skills + literals lenses), adversarially verified, 1:1 issues, repeat until a clean round.
4. **Multi-agent security review** (crypto/auth/federation/injection/erasure/supply-chain).
5. **DigitalOcean full-spectrum + attestation** ship-gate (money-gated `AI_MEMORY_OPERATOR_DO_SPEND_APPROVED`; mandatory teardown §7.6).
6. **AI-NHI dogfood** (3-green + live-DB migration rehearsal on a backup).
7. **3×7 documentation drive** (release-notes, CHANGELOG, ROADMAP §27→shipped, Pages, evidence pages, docs-vs-SSOT green).
8. Post **SHIP-RECOMMENDED** to #1940 → **STOP** for the operator tag.

## Hard constraints
- NEVER cut the tag / bump manifests / run `release.yml` (operator-gated).
- No `/tmp` scratch (`.local-runs/` only). Monitor disk (<25G WARN, <10G STOP). Never print secrets.
- DO spend gated + mandatory teardown. Sonnet never on signing/gov/federation-auth internals.
- Every `Tn` crossroads → 2×5 vote, cited.

## Loop checkpoint discipline
Each iteration: pick the next item(s) by tier order (A→D, dependency-aware), make concrete progress, `memory_store` a checkpoint, re-arm. Update this file's status as items close. Never end without a checkpoint.
