## Adjudication W3-B
**Final vote:** B — land the dispatch pre-gate, a positive-inventory guard and one read predicate inside the freeze; the capability-typed `Authority` on write primitives and any crate/module move go to v1.1.0, because 7 of the 10 chain-3 issues were object-predicate/binding bugs that neither a grep guard nor a write-capability type would have caught.

**Dispute rulings (file:line):**
1. **~229k (39%) test, ~358k production.** Heuristic: indentation-matched `#[cfg(test)] mod {}` blocks + single `#[cfg(test)]` items + 9 test-only files declared `#[cfg(test)] mod x;` (21,824 lines, e.g. `src/handlers/tests.rs` 17,129). Brace-only inline count is 197k. SIZE-FACTS 331k/56% is wrong (first-marker heuristic misfires where `#[cfg(test)]` precedes a `mod x;` line, `src/mcp/mod.rs:67`); W2-B 254k/43% is wrong; W2-A 227,680/38% is within 1%. Top-20 in-file test modules = 92k, not 117k.
2. **W2-C is right.** Key enrollment is default-REQUIRED: the `(None,None)` arm refuses `peer_not_enrolled` (`src/handlers/federation_signing_check.rs:2466-2486`; default `true` on unset `:2508-2513`). Only the authorship/namespace allowlist is opt-in (`src/federation/peer_attestation.rs:143-144`). W1-A conflated them.
3. **Inert.** `inbound_write_namespace_authorized` returns `true` on `!has_allowlist()` (`src/federation/receive_auth.rs:1226-1228`) before reading `require_push_namespace_scope`; the pin (`src/security_profile.rs:51`) never references `PEER_ATTESTATION` (grep: 0).
4. **A default install denies nothing.** `PermissionsMode::default() = Advisory`, "log a warning and allow" (`src/config.rs:6128-6143`); no-rule default `Allow` (`src/governance/mod.rs:667-669`). Production `Permissions::evaluate`: 17 sites in 15 files (e.g. `src/mcp/tools/archive.rs` x2, `src/handlers/route_1111.rs:1288`), not 47/21. `enforce` pinned only under asi-hard (`security_profile.rs:63`, not :59).
5. **2/10 no caller resolution; 7/10 resolved but wrong predicate/binding; 1 docs-only.** From `git show <merge>^1:<file>`: none — #3379 `src/mcp/tools/share.rs`, #3381 `auto_tag.rs` (0 markers each). Resolved-but-wrong — #3380 `consolidate.rs^1:74,90,127`; #3382 `archive.rs^1:66,134,207,220`; #3383 same file (purge not admin-gated); #3386 `kg_query.rs^1:84` resolves, `is_readable_on_query` on one path only (`:119,198`); #3498 `load_family.rs^1:379`, `find_paths.rs^1:98`, `lineage.rs^1:102`, `kg_timeline.rs^1:83` on the old `is_visible_to_caller`; #3499 `recall.rs^1:1053,1123` took `as_agent` as a self-claim; #3406 `handlers/capture_turn.rs^1:78,182` resolved and evaluated but `capture_turn/attestation.rs` did not exist (attestation class). #3397 (`1b4a18867`) is doc text only. W2-B's 4/10 is wrong (kg_query resolved; export_reflection is #3551).
6. **Extraction relocates ~132k production lines, removes zero**: storage 36,228, store 48,876, governance 12,924, identity 13,291, federation 12,734, portability 3,649, signed_events 3,393, visibility 755. Alternatives: 20 largest test modules = 92k relocated (net 0); `tests/*_pg.rs` 31 files/14,528 lines (up to ~14k removable); 107 `dispatch_*` wrappers, 113 `ctx.conn` reads, `src/mcp/mod.rs:1511-3372` (~1.9k); HTTP twins: 6/42 handler files use `mcp::tools`, 98 `StorageBackend::Postgres` branches in 28 handler files (fold size unverified).
7a. **Guard redefined now; type boundary v1.1.0.** `skip_path` exempts `/mcp/tools/`, `/cli/`, `src/handlers/`, `src/governance/`, `src/federation/`, `src/portability/` (`tests/record_stop_structural_b7.rs:117-138`). `ToolDispatchCtx` has `pub conn: &rusqlite::Connection` and no caller field (`src/mcp/mod.rs:1438-1480`); 71 tools/handlers files take a raw `Connection`; 431/811 test files import `storage`/`store`, 272 open raw connections; `storage/mod.rs` has 233 `pub fn`. Privatising `conn` or threading `Authority` through those is mass signature churn — outside the freeze. Fits the freeze: (i) mandatory principal+binding resolution at the two chokepoints, `lookup_dispatch` (`src/mcp/mod.rs:3357`) and the HTTP middleware layer (`src/lib.rs:1576-1579`), attached to ctx; (ii) guard = positive inventory over `TOOL_DISPATCH_TABLE` (`:3053`) and the route table, no `skip_path`; (iii) `is_readable_on_query` (`src/visibility.rs:703`) pinned on every read/list funnel — that, not the resolver, catches the #3386/#3498/#3499 class.
7b. **Not landed.** Tip and both lane branches (`origin/fix/3578-wake-hub-process-isolation`, `origin/fix/3578-sysusers`) hold only `tests/wake_hub_process_isolation_3578.rs`, a systemd-unit grep (`:4-12,35-42`). No import gate over `src/wake_hub/**` in `tests/`. Footprint is small (`use crate::` → `wake_hub` 6, `identity` 4) but ungated.
8. **Counts hold; file count does not.** Production `unsafe {/fn/impl}` outside test modules and `#[test]` fns: 51 in 18 files (`src/identity/key_inventory.rs` 13, `src/governance/deferred_audit.rs` 11, `src/audit.rs` 6, `src/wake_hub/startup.rs` 6, 14 files with one); test-side 731. No `forbid/deny(unsafe_code)`, no `deny.toml`/cargo-vet (`supply-chain/` holds only `build-script-vetting.json`), no `undocumented_unsafe_blocks`.

**Ballot errors found:** SIZE-FACTS 56%; W2-B 43%, 117k, 4/10; W1-A "enrollment opt-in" + amendment 3; W2-A claims the type boundary would have stopped chain 3 (7/10 were read-side); W2-C `:59` (`:63`); assessment 47/21 (17/15; "31" not verified); Conductor "8 files" (18); brief lists #3397 as a leak.

**Amendments that survive:**
1. Production-only counts: 17 authz sites/15 files, 51 unsafe/18 files, 39% test share.
2. #3549 resolver = `Authority{principal, binding, admin}` at both chokepoints; no `decision`, no K9 `Op` growth.
3. Guard = positive inventory, no `skip_path`; allowlist-with-reasons frozen as the boundary spec.
4. Pin `is_readable_on_query` on every read/list funnel; retire public `is_visible_to_caller`.
5. Federation stays its own boundary; "key enrollment default-on, authorship allowlist opt-in"; asi-hard requires `AI_MEMORY_FED_PEER_ATTESTATION` when peers exist.
6. Record the Advisory default; #3125 ruling states the Standard posture.
7. #3199: sign the snapshot, cover `--skip-verify` and the manifest-less path, scope to SQLite.
8. Schema row: 25 raw hits all test/comment; add a cfg(test)-aware DDL gate.
9. Do not cite #3578 item 1 until its test exists.
10. v1.1.0 = capability-typed `Authority` on write primitives + private `conn`, then module move; size work (pg-twin parametrisation, wrapper macro, test relocation) filed separately.

**Amendments rejected:** W2-A #1 in v1.0.0 (mass churn, misses the dominant read-side class); W2-B #1 striking "boundary" (keep boundary, drop "kernel"); W1-A #3 as worded; W2-A #3 as a GA blocker.

**Kernel facets:** inside — schema ladder (`migrations.rs`), `merge_inbound`/`import_full_envelope`, writer attestation (`identity/attest.rs`), principal+binding resolution at both chokepoints, the single read predicate, signed restore apply, `receive_auth` as the peer boundary. Outside — 104 tool wrappers, HTTP mirrors, CLI, recall/rerank/embeddings, wake-hub, docs/SDK.

**Size answer:** src/ is 587k lines: 229k (39%) tests, 358k production. Extraction moves ~132k and deletes none. Real reductions: pg-twin parametrisation (~14k), dispatch macro (~1.9k), storage→trait fold (98 twin branches, size unverified); relocating 92k of tests changes location only.

**Language answer:** Rust stays: 51 production unsafe blocks in 18 files, 731 test-side. In-freeze hardening: `#![forbid(unsafe_code)]` on the boundary modules (only `storage/sqlite_integrity.rs:423` sits there), `undocumented_unsafe_blocks=deny`, a cargo-deny gate; Miri/Kani on resolver+apply is v1.1.0.
