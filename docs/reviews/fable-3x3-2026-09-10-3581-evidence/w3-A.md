## Adjudication W3-A
**Final vote:** B — land the dispatch pre-gate, a positive inventory guard, #3199 and #3124 inside the freeze; the type-enforced `Authority`/private-connection boundary is the v1.1.0 target, not a crate move.

**Dispute rulings (file:line):**
1. Brace-matched every `#[cfg(test)]` item in src/ (491 files, 587,693 lines): 208,081 inline + 21,832 in 8 whole-file test modules (`#[cfg(test)] mod x;` at src/handlers/mod.rs:386-388, src/mcp/mod.rs:67-68,80-81, src/lib.rs:873-874, src/daemon_runtime.rs:9064-9066, src/mcp/tools/store/mod.rs:1272-1274) = 229,913 (39.1%); production 357,780. W2-A upheld; SIZE-FACTS (56%) and W2-B (43%; "20 files hold 117k" — top-20 inline modules sum 75,837) rejected.
2. W2-C right. Key enrollment default-REQUIRED: `require_peer_enrollment_enabled()` true on unset, `(None,None)` arm refuses `peer_not_enrolled` (src/handlers/federation_signing_check.rs:2459-2486, :2508-2513). Authorship/namespace allowlist is zero-config faith-based (src/federation/peer_attestation.rs:143-144).
3. Inert, confirmed: `!attest_cfg.has_allowlist()` returns true before `require_push_namespace_scope` is read (src/federation/receive_auth.rs:1226-1228); src/security_profile.rs pins the knob (:51) and has 0 hits for PEER_ATTESTATION.
4. W2-C wrong on the default. `PermissionsMode::default()=Advisory` (src/config.rs:6146-6150) is serde-only; effective mode is `effective_permissions_mode()` → `resolve_v07_default_mode(None)` → Enforce (config.rs:8671-8678; src/governance/mod.rs:844-846, 864-874), installed at boot (src/main.rs:180); pre-boot fallback Advisory (config.rs:6740). But with no rules/hooks Enforce resolves Allow (`mode_default_for`, governance/mod.rs:662-669): a default install denies nothing until rules exist — by design, not Advisory. Production `Permissions::evaluate(`: 15 sites/14 files (12 in src/mcp/tools; storage/mod.rs:9134; handlers/route_1111.rs:1288; handlers/capture_turn.rs:203); the "47" includes 15 governance/mod.rs tests.
5. #3397 is a docs truth-fix (merge 1b4a18867), not a leak. Of 9: NO caller resolution pre-fix — #3379 mcp/tools/share.rs (0 resolver calls at 70d08aa8c^1), #3381 auto_tag.rs (0), #3498's load_family.rs (0). Resolved caller, wrong object predicate — #3380 (consolidate.rs 2 caller sites), #3382/#3383 (archive.rs 2), #3386 (kg_query.rs 8 — W2-B's "zero" wrong), #3406 (handlers/capture_turn.rs 2), #3498 (find_paths/kg_timeline/lineage already called `is_visible_to_caller(&mem, caller)` at pre :98/:83/:102, anchor only), #3499 (all had caller). Score ≈2.5 "forgot" vs 6.5 "wrong predicate". W2-B's thesis (dispatch resolves principal+binding+admin; object predicate per handler) upheld; his 4/10 and export_reflection (#3551) not.
6. Extraction relocates: would-be core dirs (storage, store, governance, identity, federation, portability, signed_events, visibility) = 192,954 lines incl. tests, 0 removed. Real removers: tests/*_pg.rs 31 files/14,528 lines (≤14.5k); `fn dispatch_*` 104 wrappers at src/mcp/mod.rs:1511-2955 (~1,445 lines, macro saves ~1.2k); HTTP/MCP twins create.rs 2,212 + bulk.rs 1,964 vs store/mod.rs 1,274, 6/42 handler files reference mcp::tools, 98 `StorageBackend::Postgres` branches in 28 handler files vs 0 in mcp/tools (largest reduction, unquantified). 
7a. `skip_path` confirmed (tests/record_stop_structural_b7.rs:117-138). `ToolDispatchCtx.conn: &rusqlite::Connection` is pub (src/mcp/mod.rs:1437-1438), 113 `ctx.conn` uses, no caller field (only `mcp_client`); 423/811 tests import ai_memory::storage|store, 251 open rusqlite::Connection. #3549 not landed (`resolve_caller_authority` only in docs/reviews). In-freeze: (a) pre-gate at both chokepoints (mcp/mod.rs:3360-3364 table loop; src/lib.rs:1576-1579 middleware) producing `Authority{principal,binding,admin}` as an additive ctx field with private ctor; (b) guard redefined as a positive inventory over all 104 TOOL_DISPATCH_TABLE entries and route registrations, each declaring resolver-call or allowlisted reason — no skip_path. v1.1.0: write primitives take `&Authority`, `conn` leaves ctx (104 wrappers + 423 tests = signature churn).
7b. Not landed: tests/wake_hub_process_isolation_3578.rs is item 3 (systemd unit grep, :4-12); no tests/ file gates imports of src/wake_hub/**; origin/fix/3578-wake-hub-process-isolation and origin/fix/3578-sysusers add only that file.
8. Counts corrected: 61 production `unsafe` sites in 20 files (key_inventory.rs 13, deferred_audit.rs 13, audit.rs 6, wake_hub/startup.rs 6 — libc fd/uid/rlimit, e.g. startup.rs:89,144,237), 723 test-only in src (614 env set/remove_var) + 649 in tests/. No `forbid/deny(unsafe_code)` anywhere; Cargo.toml [lints.clippy] only `too_many_lines` (:630-631); cargo-audit in CI (ci.yml:1379, .cargo/audit.toml), no deny.toml/cargo-vet. Hardening proposal sound.

**Ballot errors found:** SIZE-FACTS/W1: 56% (first-marker hits `mod *_tests;` at mcp/mod.rs:67). W1-A: "enrollment opt-in". W2-B: 254k/117k; kg_query "zero"; export_reflection. W2-C: default is Enforce, not Advisory. W2-A: "#3549's guard" is b7 (#3549 unlanded); 101→104 wrappers. Conductor: 52/8 unsafe understated (61/20).

**Amendments that survive:**
1. Replace 47/31 with 15 evaluate sites/14 files and ~8 attestation sites, method cited.
2. Federation cell: key enrollment default-required, sig+nonce default-on, allowlist opt-in, asi-hard cannot pin it; asi-hard must require PEER_ATTESTATION when peers exist.
3. Authorization row: effective default Enforce (config.rs:8671-8678) but Allow absent rules; #3125 ruling states governed-namespace defaults.
4. #3549 = `Authority{principal,binding,admin}` at both chokepoints; object predicate per handler; positive-inventory guard; pin read/list funnels on `is_readable_on_query`.
5. Federation receive is a separate boundary with its own guard.
6. #3199: sign snapshot+manifest; `--skip-verify` refused under asi-hard; SQLite scope; pre-migration snapshot path.
7. Schema row: 25 files all cfg(test)/comment; CI guard on non-test DDL outside ladder.
8. Falsifier 4: quote five reasons; #2437 independent blocker.
9. v1.1.0 item = type-enforced Authority + private conn + storage/mod.rs→trait fold (98 twins), before N14/N16 last binary change; SIZE-FACTS → 39%.
10. Do not cite #3578 item 1 until its test exists.
11. `#![forbid(unsafe_code)]` on boundary modules, `undocumented_unsafe_blocks=deny`, cargo-deny gate.

**Amendments rejected:** W2-A #1 in v1.0.0 (remove `conn` now) — freeze violation. W2-B #1 "strike kernel" — naming only. W2-C #2 as written — factually wrong. W2-B's 117k figure. W1-A #3 as stated — enrollment already required.

**Kernel facets:** inside — durable commit (storage txn + ladder v98), writer attestation (identity/attest.rs, signed_events), authority resolver + `Permissions::evaluate` + visibility predicates, schema ladder, apply paths (portability import, federation receive_auth/merge_inbound, backup restore). Outside — 104 tool wrappers, HTTP handlers, CLI, embeddings/reranker/LLM, wake_hub sidecar, curator/autonomy, docs/SDK.

**Size answer:** src/ is 587,693 lines, 229,913 (39%) test, 357,780 production; tests/ adds 351k. A core extraction relocates ~193k lines and removes none. Verified removers: pg twins ≤14.5k, dispatch macros ~1.2k, and the unquantified HTTP/MCP + 98 backend-branch fold; moving in-file tests only shifts 230k between directories.

**Language answer:** Rust stays: 61 production unsafe sites in 20 files, all libc/fd plumbing, none in the authority or apply paths; the hardening (forbid on boundary modules, documented-unsafe lint, cargo-deny) is cheap and additive; Miri/Kani not verified feasible.
