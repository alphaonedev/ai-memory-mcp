## Adjudication W3-C
**Final vote:** B — land the boundary (#3549 resolver + positive-inventory guard, #3199, #3124), no crate split in the freeze; extraction relocates 132k production lines and deletes none, so v1.1.0 is reduction work, not "extract core".

**Dispute rulings (file:line):**
1. **W2-A right; SIZE-FACTS and W2-B wrong.** Method: python3 over src/**/*.rs; each `#[cfg(test)]` + inline `mod x {` brace-matched (strings/`//` stripped); out-of-line `#[cfg(test)] mod x;` files counted whole. In-file 206,576 + 8 out-of-line files 21,832 (`handlers/tests.rs` 17,130 via `handlers/mod.rs:386`) = 228,408 of 587,693 (38.9%); production ~359k. W2-A's 227,680 is within 0.3%. First-marker heuristics fail: `mcp/mod.rs:67` is `mod *_tests;`; `postgres.rs` production follows its first test module (35,265/7,145).
2. **W2-C right.** `(None,None)` arm refuses `peer_not_enrolled` under `require_peer_enrollment_enabled()` (`federation_signing_check.rs:2458-2486`; unset→`true` :2508-2513). Only the authorship/namespace allowlist is opt-in (`peer_attestation.rs:143-144`).
3. **Inert, confirmed.** `return true` on `!has_allowlist()` (`receive_auth.rs:1226-1228`) precedes the first read of `require_push_namespace_scope` (:1323). `security_profile.rs:362` pins the knob, never `PEER_ATTESTATION`. Posture gate opt-in (`enterprise_federation_posture.rs:779-783`).
4. **Conclusion right, mechanism wrong.** Production `Permissions::evaluate` sites = **15** (12 in `mcp/tools`, `storage/mod.rs:9134`, `handlers/route_1111.rs:1288`, `handlers/capture_turn.rs:203`). A default install denies nothing because it ships no rules: `mode_default_for`→`Allow` (`governance/mod.rs:667-669`). Advisory does NOT downgrade an explicit Deny — `evaluate_with` returns `Deny` before consulting mode (`:537-545`), only keeping Ask as Ask (`:654-658`); callers hard-fail on Deny (`archive.rs:160-166`). `config.rs:6128-6131` misdescribes this.
5. **W2-B overstated.** Pre-fix caller-resolution identifiers per touched file: #3379 `share.rs` **0**; #3380 `consolidate.rs` 3; #3381 `auto_tag.rs` 1; #3382/#3383 `archive.rs` 4; #3386 `kg_query.rs` 10 (`is_readable_on_query` at :119,:198); #3406 `capture_turn.rs` 3; #3498 `handlers/kg.rs` 24; #3499 `handlers/recall.rs` 20. #3397 (`7b25a0f28`) is docs-only. Tally: 1/9 no resolution, 8/9 wrong predicate/binding; #3383 (admin), #3406 (attestation), #3499 (`as_agent`) are dispatch-resolvable.
6. **Relocates.** Production lines: storage 36,241 + store 48,884 + governance 12,932 + identity 13,313 + federation 12,758 + portability 3,655 + signed_events 3,394 + visibility 756 = **131,933** moved, 0 deleted. Alternatives: (a) 20 largest in-file test modules → tests/: 75,296 lines (not 117k), size-neutral; (b) 31 `tests/*_pg.rs` twins 14,528 lines; (c) 103 `dispatch_*` wrappers ~842 lines, macro saves <1k; (d) HTTP/MCP twins `create.rs` 1,962 + `bulk.rs` 1,776 vs `store/mod.rs` 1,190; (e) 98 `StorageBackend::Postgres` branches in 28 handler files + legacy `storage/mod.rs` 23,579 prod behind the trait — the only >10k candidate (overlap unverified).
7a. `skip_path` confirmed (`tests/record_stop_structural_b7.rs:117-138`), exempting every chain-3 layer. #3549 already says "over the generated inventory, zero mutating allowlist entries": the guard must positively check every `TOOL_DISPATCH_TABLE` entry (`mcp/mod.rs:1403`) and `.route()` registration passes the chokepoint (`mcp/mod.rs:3362`, `lib.rs:1578`). Type enforcement (`conn` out of `ToolDispatchCtx`, `mcp/mod.rs:1438`) touches 103 wrappers, 70 handler files, 443 tests → v1.1.0.
7b. **Not landed.** Tip and both `origin/fix/3578-*` branches carry only `tests/wake_hub_process_isolation_3578.rs`, a systemd-unit scan (:37-40); no import gate over `src/wake_hub/**`.
8. See Language answer.

**Ballot errors found:** W1-A: "enrollment opt-in"; "~20 authz sites" (15). W2-B: 254k/43%; "117k in 20 files"; "4/10 zero resolution" (kg_query had predicates; export_reflection/load_family not in the list). W2-A: "9 sites in mcp/tools" (12: missed `delete.rs:169`, `update.rs:409`, `store/mod.rs:529`); "~115k" (132k). W2-C: Advisory mechanism. Assessment: 47/21 and 31 (7 calls + 14 literals); #3578 cited as landed. Brief: #3397 is docs-only. W1-B/W1-C DDL classification not re-verified.

**Amendments that survive:**
1. Replace counts: 15 authz sites, 7 attestation calls, 228k/38.9% test share.
2. Resolver = `Authority{principal, binding, admin}` + op class at both dispatch points; drop `decision`; object predicates stay per handler, pinned to `is_readable_on_query` (W2-B 2-3).
3. Guard = positive inventory (7a), no `skip_path`; type-enforced `Authority`/private `conn` filed for v1.1.0 (W2-A 1 rescoped).
4. Federation wording per ruling 2; asi-hard requires `AI_MEMORY_FED_PEER_ATTESTATION` when a peer is configured; federation keeps its own guard (W2-C 1,4; W1-A 2).
5. Governance row: no rules→Allow; reconcile `config.rs:6128-6131` with `evaluate_with`; #3125 states the Standard default (W2-C 2).
6. #3199: sign the snapshot, refuse `--skip-verify` under asi-hard, cover the manifest-less pre-migration path (W1-A 4, W1-B 4, W2-C 3).
7. DDL classification stated + CI pin (W1-B 1-2, W1-C 3).
8. #3578 item 1 lands before citation (W2-A 3).
9. v1.1.0 = reduction per 6(e),(b),(d); extraction after, under W1-C 4-5.
10. Language hardening (below).

**Amendments rejected:** W2-A 1 in v1.0.0 (mass churn); W2-B "117k"/"4/10"; W1-A 3 as worded (superseded); W2-B 1 retitle (cosmetic); Miri over apply (C FFI). W1-C 1 carried unverified.

**Kernel facets:** Inside: durable commit (`storage/migrations.rs:995` ladder, store commit paths, #3124); writer attestation (`identity/attest.rs`, #3419); authorization (dispatch resolver, `Permissions::evaluate_with`, `is_readable_on_query`; federation `receive_auth`/`signing_check` as peer-equivalent); schema (ladder + DDL pin); fork-safe apply (`portability/import.rs`, `storage::merge_inbound`, signed `cli/backup.rs`). Outside: tool bodies, HTTP handlers, CLI, wake_hub, LLM/embeddings/reranker, curator, workers.

**Size answer:** src/ 587,693 lines: ~359k production, 228k test (38.9%). Extraction moves 131,933 production lines and deletes none. Moving tests out drops src/ by 75k (top-20) or 228k (all); the repo does not shrink. Real deletion: pg-twin parametrisation (≤14.5k), HTTP→MCP delegation (≤3k), legacy `storage/mod.rs` fold (~20k, unverified); wrapper macros <1k.

**Language answer:** Rust stays. Production `unsafe` (code lines outside any cfg(test) region/fn; comments and the `skill_export.rs:550` string excluded): **53 in 18 files** — Conductor's 52 holds, "8 files" does not; mostly libc (`key_inventory.rs`, `deferred_audit.rs`, `audit.rs`, `wake_hub/startup.rs`), model mmap (`embeddings.rs:1074`, `reranker.rs:1211`), `vectorlite.rs:207`, one env write (`daemon_runtime.rs:3277`), one `no_mangle` (`lib.rs:598`). Test-side: 732 in src, 658 in tests/. No `forbid(unsafe_code)`; `lib.rs:11` blanket `#![allow(clippy::pedantic, clippy::all)]`; no `deny.toml`, no cargo-vet (`supply-chain/` holds only build-script vetting), no Miri/Kani; cargo-audit IS in CI (`ci.yml:1384-1385`, two ignores in `.cargo/audit.toml`) — omitted by the Conductor. Crypto deps (`Cargo.toml:121-207`): `ed25519-dalek 2`, `x25519-dalek 2`, `chacha20poly1305`, `hkdf`, `hmac`, `sha2`, `blake3`, `subtle`, `zeroize`, `rustls/ring`; the Conductor's list is absent from issue-3581.md — not verified. Hardening: v1.0.0 — cargo-deny config; `#![forbid(unsafe_code)]` on zero-unsafe boundary modules (`store`, `federation`, `portability`, `visibility`, `signed_events`); module-level `deny(clippy::undocumented_unsafe_blocks)` on `identity`/`governance`/`storage`. v1.1.0 — Kani/proptest over `evaluate_with` and the resolver; Miri over apply rejected (C FFI).
