# Full seven-juror panel — Wave 2 ballot C

Reviewer: GPT 6 Astra, enterprise/federation/reliability juror C. Reviewed source `87f86a0a1399d8282a60690ce463cba2ba688ebe`. This is the full-panel Wave 2 ballot; earlier three-juror cross-examination and preliminary draft review do not count toward these 21 ballots.

I read all seven Wave 1 files in full: retrieval, security, enterprise, continuity, cognition, operations and architecture. I also read both current assessment and test-plan drafts in full. New structural discovery used CodeGraph explore before focused reads. Exact new coverage is appended to `source-coverage-enterprise.json`; no new service mutation, remote fault test, backup restore or PostgreSQL workload was executed.

## Votes

| Proposition | Vote | Confidence |
|---|---|---|
| Material operational value for AI agents | **YES, within a supported and wired deployment contract** | High |
| Demonstrated universal grand slam / absolute first-choice endpoint | **NOT PROVEN** | High |
| Broad Fortune 500/government mission-critical bet-the-farm readiness established | **NOT PROVEN** | High |

Confidence concerns the evidentiary judgment, not an estimated probability of system safety. I would withhold broad authorization now. This does not imply every constrained deployment is unsafe or should wait for universal superiority.

## Accepted challenges and why

**A — read fidelity is an agent workflow defect.** The independent fixture combines current content with default version 1 and missing CID, corroborated by current SQLite projections. An additional canonical get is an effective workaround and CAS rejects stale versions; that mitigation reduces corruption risk but adds calls and leaves the returned object misleading. Do not carry this reproduced SQLite fault into PostgreSQL without another reproduction. A nonempty nearest neighbor is also not proof of a relevant answer; a configurable abstention contract is reasonable.

**B — attestation decoration and alternate authorization paths need exact object/surface language.** An incident signed link cannot authenticate both endpoint payloads. This is an interpretation defect; it is not demonstrated ranking or signature bypass. Source-confirmed local MCP share/admin-purge issues are material when a shared profile exposes those operations. They are not a demonstrated PostgreSQL HTTP exploit; current unsupported routes and trusted admin checks must remain credited. Generic signed policy is useful, but does not replace invariant checks at every alternative entrance.

**D — capture success is a different contract from an accepted MCP envelope.** The isolated pending/ask probe concretely demonstrates false capture acknowledgment in two Python transports. The server still enforces governance. A host that believes a pending checkpoint was persisted can lose its recorded mission even when every committed PostgreSQL row is durable. This is an important application-level continuity limit independent of native engine reliability. D's additional readiness-timeout observation limits the producer's aggregate PASS and should be repaired; the supplied successful timing rows still deserve positive credit.

**E — outcome evaluation must isolate complete swarms, not agent arms sharing state.** Accepted and now reflected in the plan. Stored experience, signed dependencies, exposure folding and real rewind are useful implementations. None alone proves improved decisions, independent corroboration or reversal of an external side effect. The old 0/8 mission score remains a diagnostic failure of that run, not an estimate of universal product success or proof the engine caused eight failures. Stronger positive operational findings can coexist with lack of a controlled task-outcome advantage.

**F — SQLite recovery authenticates structure more strongly than origin.** The co-located unsigned hash/manifest threat requires backup-directory write access; it is still material when an autonomous recovery system trusts that directory as a source of mission authority. Consistent snapshot, staged replacement and PostgreSQL refusal are substantial fixes that narrow the negative claim. Directory fsync/unlink warnings and a preflight liveness check do not establish a fully fenced, crash-durable promotion. Require machine-readable degraded conditions and a tested exclusive promotion boundary. F read the complete production implementation; I am not claiming that full coverage myself.

**F — release provenance does not prove qualification.** My direct preflight/supply-chain read confirms a commit SHA is resolved but the tag name is checked out again for the gate, and annotation/signature is not verified by the named preflight. F's full-file examination supplies the broader lack-of-qualification-gate and unpinned-tool findings. Existing 35 branch protection contexts and enforced signed commits are meaningful counterevidence to an unguarded-repository claim; they do not by themselves bind every tag-dispatched build to the same tested immutable commit. No tag attack or published artifact compromise was observed.

**G — convenience PostgreSQL export is a live scan, not a shared snapshot.** Independently confirmed through CodeGraph's complete keyset helper and direct complete caller/HTTP handler reads. Each page uses the pool, and links are fetched afterward in another call. The single as_of value stabilizes expiry filtering only. Concurrent edits can yield a combination not representing one committed state. Pagination fixes the old 1,000-row cap and advances through skipped raw rows; those fixes stand. The surviving Vec and log-only withholding do not give an agent per-run partiality accounting.

## Rejected overextensions and disagreement limits

1. **Reject “Pg/AGE/vector are only installed, therefore there is no enterprise application evidence.”** Our SQL probes alone are substrate checks, but historical Tracks A/B/D contain real application/agent evidence, issue #3424 reports current native-tier application parity, and the exact-SHA certified workflow succeeded on its second attempt. Historical scope and source pins matter; their age does not erase execution evidence.
2. **Reject “unsigned SQLite backup or mixed-state JSON export proves native PostgreSQL recovery broken.”** Those are different recovery paths. The export already says portability_complete:false and PostgreSQL-native backup is explicitly delegated. The outstanding enterprise qualification is an authenticated composed recovery point for PostgreSQL data, governance sidecar, key/policy history and application state, followed by a correct mission action. It is not a demand that convenience export become the native backup system.
3. **Reject “no new chaos soak means broken durability.”** Absence of that new evidence limits authorization scope; it is not a reproduced failure. The 333/333 acknowledged rows across three daemon kills are real positive evidence for the bounded daemon-kill scenario. They do not test loss of the database process/disk, exact full record bytes or model continuation.
4. **Reject upgrading local-only 202 receipts to quorum promises.** Local availability during a partition is an intentional useful behavior. Test loss against its advertised fault class; a mission requiring quorum must not proceed on a weaker receipt without explicit policy. The revised plan now makes this distinction.
5. **Reject feature breadth or code size as the primary release blocker.** Explicit unsupported cells are acceptable for a declared supported profile. A wholesale large-module rewrite at release freeze can increase risk. Fix common domain invariants and prove contracts before incremental structural extraction.
6. **Limit F/G's broad-readiness NO to its stated authorization meaning.** I record NOT PROVEN for the same practical decision because neither their source-derived residuals nor our artifacts demonstrate every bounded mission is unsafe. This is semantic dissent, not a disagreement that broad bet-the-farm reliance is currently unjustified.

## Assessment/test-plan disposition

The revised drafts incorporate the material corrections: precise kill-to-readiness timing, pending/ask capture receipts, whole-swarm randomization, native-vs-CLI recovery separation, meaningful release provenance credit, explicit local durability/fault classes, legitimate refusal telemetry, and Big-10 false-green oracles. The plan's dedicated infrastructure and declared risk-based SLOs are feasible as a staged campaign, rather than a promise to perform destructive qualification on everyday nodes now.

One bookkeeping condition remains: the evidence paragraph must not finally claim 21 complete ballots until all full-panel Wave 2 and Wave 3 records actually exist. The process section currently marks the final tally pending, which is honest for a draft. Source coverage must retain exact read ranges and partial-file labels; lexical scanning is not semantic whole-repository review.

## Strongest counterargument and falsifiers

The strongest positive challenge is that a responsible operator can choose a narrow profile, use native database recovery, restrict exposed operations, pin actual adapter versions, require validated receipts and already obtain substantial benefit. I accept that. Enterprise adoption does not require ai-memory to win every no-memory comparison or be the sole root of business authority.

To reverse the readiness judgment for a declared profile: bind one immutable binary/configuration to passing surface conformance, tenant refusal pairs, exact memory/version/ownership state, authenticated cross-store restore, declared W/N partition/rejoin behavior, duplicate-effect reconciliation and fresh-agent correct continuation. Record queues and convergence under the intended load. Cover the pool-direct graph-delete crash/dual-failure residual in a dedicated fixture. An external operator can supply parts of this contract; they need not all be implemented inside the crate.

To reverse universal superiority skepticism: reproducible held-out matched-budget mission results across named host/model families, with correction adoption, harmful-action rate and total cost, must show a meaningful advantage over competent files+CodeGraph and ordinary retrieval baselines. Model preference questionnaires or unanimous juror votes cannot supply that evidence.

I would withdraw the export snapshot finding if an enclosing shared transaction/snapshot across the actual pool calls and link call were demonstrated. I would withdraw a release qualification gap if an enforced external rule bound the published artifact's immutable source to the required passing results. A documented intention, tag name, function name or old green badge alone is not that falsifier.
