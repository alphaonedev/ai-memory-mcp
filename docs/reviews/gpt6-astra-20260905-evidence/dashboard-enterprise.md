# Dashboard enterprise evidence review — GPT 6 Astra

Read-only access to f2 /home/fate_two/v07/v09-dev authorized by user. No tests, kills, restarts, writes or remote configuration changes executed. Inspected copied public federation-state.json, datatier-state.json, northstar-state.json, thirty module-scaling result JSONs, five continuity JSONs, module-scaling.py and continuity-cycle.py, four big10 summary JSONs and relevant big10-regression.sh producer lines. Local copies in dashboard-enterprise-artifacts/ and root f2-runs/. Source review remains87f86a0a; remote run artifacts below have their own provenance, not silently current source acceptance.

## Material update to enterprise assessment

There is more real Pg/agent-facing performance and restart evidence than the earlier committed-doc corpus alone showed. Credit it. A current-session SQL-only observation must not be used to erase actual September1 runtime results. Conversely, these raw artifacts do not bind every dashboard green cell to the current release SHA or prove a large autonomous NHI mission workload.

## Capacity: independently reproduced the displayed arithmetic

At2026-09-01T21:54:50/51Z, eight one-module32-client files sum2936.1ops/s:31ba9a,12a432,28ebe6,7a34f4,7d7500,360da1,0215da,af0cf2. At21:56:34/35Z eight two-module32-client files sum4602.3ops/s:f85993,f2f4b6,25c45b,832405,182010,3c7a10,fd6705,0ce185. These exactly explain the dashboard2936 versus4602 headline at256clients. All sixteen selected files report empty error maps. Per-process maximum p95 GET153.5ms/list85.2/search85.4 in one-module group;111.3/97.6/95.0 in two-module group. Do not average percentiles into a fleet percentile.

Producer module-scaling.py registers distinct agent identities and signing keys, seeds two short rows per identity, and loops GET/list/keywordsearch under asyncio concurrency. Two-module bases are local HTTP f2 plus remote HTTPS f1; HTTPS options use swarm TLS config. This is real SDK/API traffic using independent scoped principals across two endpoints. It is not256independently reasoning models, semantic/hybrid recall, sustained mixed write/read mission behavior, or proof of replicated per-write scaling. Agents are load-generator identities. Default timed rung is10seconds; artifact lacks actual duration parameter/start time, source revision, binary hash, cert/env manifest and host pressure. Matched finish timestamps support concurrent grouping but exact overlap/invocation needs the runner log. State the bounded result and keep the headline's workload type explicit.

## Continuity: real daemon SIGKILL/readback evidence, narrower than autonomous resume

continuity-a56d9a.json2026-09-01T20:30:27Z records3cycles, all retained=true; acknowledged rows91/91,112/112,130/130, total333/333. Resume values1089.0/1014.4/996.7ms. Mission memory, inbox, mission recall and key-binding signed write all true. Three later single-cycle files13cd0d21:59:40Z,ae919f22:01:42Z,815e3623:18:47Z record121/121,131/131,110/110 respectively, all retained,1059.0/1051.7/1191.8ms. This is meaningful live local daemon-under-load recovery evidence.

Producer continuity-cycle.py explicitly kills only the serving daemon under signed-write load; Postgres is not killed, power-cut, or failed over. It waits0.5seconds, restarts via f2-daemon.sh, waits for health/embedder readiness, then records resume_ms BEFORE it verifies mission/inbox/recall/ACKreadback. Thus timing is daemon readiness from kill (including imposed pause), not an AI mission's first correct resumed action. Mission test checks returned ID, recall inclusion and inbox marker, not byte-for-byte memory/version convergence or action exactly-once continuation. User clarifies dashboard resume_ms was manually carried over from prior fleet run; we found a producer writing resume_ms to its own raw continuity files, which is consistent with manual dashboard transfer. Direct machine-bound dashboard production under#3473 remains a useful closure.

Earlier continuity-a23546.json20:29:04Z reports3retained=false,0/88,0/120,0/131 while total rows grow. Current producer verifies each ACK as its owner and explicitly comments that admin/bystander gets404 for private rows. This is strong evidence for an earlier verification-scope problem, not enough to assert it definitively without original producer revision/log. Do not either allege lost durable writes from this raw failure or silently discard it: preserve it, record why subsequent owner-scoped rerun supersedes it, and bind the correction artifact.

None of these continuity JSONs embeds binary SHA, config/triple, backend URL, test script hash or git HEAD. Current producer contains Pg URL and restart wrapper path, but present producer contents are not cryptographic proof of the historical invocation. Big10 binds related single-cycle evidence to nominal git tip23b106ad, not binary hash. No freshness claim at87f86a0a earned here.

## Green dashboard is a view, not uniform all-pass acceptance

All3enterprise public JSONs have lastUpdated:null and no per-cell producer/artifact/revision references. Federation90%=9/10, datatier93%=13/14; scaling remains refining in datatier while NorthStar calls module knee validated. Both can coexist if scopes differ, but scope is not encoded. NorthStar title/data mix functionality and correct refusals: a percentage is not probability of reliability.

Datatier line item says kg_query/find_paths/lineage onAGE; current release find_paths always routes relational traversal(src/store/postgres.rs12508+). Installed AGE and graph feature names do not prove every graph operation executes Cypher. Datatier fullSQLitetool surface is markedvalidated while note says79/104tools. This is a valid partial coverage statement, not full functional parity. The known recall DTO inconsistency independently found in this assessment is compatible with a green presence/shape test and shows why memory-contract assertions need to go deeper.

The four big10 artifacts retain non-green runs:23b106ad21:59:41Z8/10 then22:01:42Z10/10; b8023dac23:06:23Z9/11 and23:12:20Z9/11, with empty evidence for cold_restart failure. big10-regression.sh31 pipes continuity output through grep matching cycle1; failures before matching output are discarded by this summary path. Those empty cells mean failed/harness-incomplete runs requiring logs, not independently proven corruption. The later815e36 continuity success is positive scoped rerun evidence but does not remake the whole11case suite green. Script36 uses working-tree HEAD for tip; it does not hash the running daemon binary. Report selected successful runs with their successors/residuals, rather than latestdashboard equals comprehensive tipcertificate.

## Ballot effect

Enterprise VALUE remains YES and confidence increases from these real signed-client load/restart results. Broad Fortune500/government autonomous-cluster farm-betting remains NOT PROVEN. The unmet requirement is end-to-end trustworthy agent work under the selected deployment's failure envelope, not whether PostgreSQL exists or whether CRUD can run quickly. Make every acceptance claim carry producer, SUT binary/config/triple, timed workload, raw outcomes, negative controls and supersession chain. Then prove actual agent goal/checkpoint/action recovery, cross-peer convergence/deletion and measured mission latency with the same memory contracts used by supported endpoints.


Wave-three producer follow-up: the Big-10 producer was subsequently read in full. Its no-plaintext assertion accepts any non-200 HTTP response (including an answering plaintext 401), and anonymous-write assertion accepts any non-201 response (including local acceptance 202 or server failure 500). These are weak/false-green oracles, not observed daemon bypasses. See preliminary-w3-enterprise.md and the SHA-256 f2 manifest. Earlier multi-rung scaling results also include write loads: at 128 principals one module253.3 versus two modules308.3 store operations/s; the two-module32-principal rung has one ConflictError. Keep these separate from the later256-principal read headline.
