# Root issue coverage and reconciliation

All 601 root-shard issue records received bounded screening of title, body excerpts and available closure/comment updates after full-input automated analysis. This is not a claim that every comment in every long thread was semantically read. The combined four shards cover 2,406 issues without overlap; later jurors independently examined selected topics.

The root fully read the short wake/recovery and certificate carriers #3473 and #3501. Longer selected issue bodies and late updates were read to reconcile claims, including #3308, #3353 and #3424; these are not labeled full-thread reads. The exact per-record metadata and hashes are in issues-coverage.csv. The other issue ledgers name their own full-thread sets.

Material dispositions:

- #3308: the September 5 amendment moves wake-hub work into GA acceptance scope. A stale title or older v1.1 deferral cannot set the current cutline alone.
- #3501: material federation/identity changes require certificate reissue. A successful old certificate and a current successful workflow are related but distinct evidence.
- #3473: actual wake-hub acceptance must produce its own fault/load evidence. The September 1 daemon SIGKILL producer is a different test.
- #3404: the already-open SQLite projection issue is independently corroborated by this assessment's live get/search/recall probes.
- #3424: reported exact-tier application and local gate results deserve credit; root did not execute that reported full suite. Red published workflows remain a separate observation.
- #3171: closure includes a named merge and schema/handler guard evidence. Its old cluster of defects is not repeated as current merely because the historical body is severe.
- Open issues can contain merged fixes awaiting verification; closed issues can be duplicates, superseded claims or completed documentation. Neither state substitutes for source and execution evidence.

The retrievable-comment discrepancy is retained: #1388 two fewer than counter, #1470 one fewer, #3266 one fewer. Direct requests did not recover the four missing comments. Raw issue data was retained locally for analysis and is not republished wholesale.
