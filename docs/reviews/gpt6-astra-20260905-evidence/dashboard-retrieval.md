# Dashboard retrieval and agent-outcome evidence — GPT 6 Astra

Evidence scope: fully read all 104 MCP tool records and 22 swarm tool records in parent-downloaded JSON snapshots, including every supplied note (the upstream notes themselves truncate tool responses). Fully read dashboard-state.json `nhiAudit`. These are historical execution summaries, not tests freshly executed by this juror. The source snapshots are under `.local-runs/astra-evidence/`; original dashboard files are on f2 at `/home/fate_two/v07/v09-dev/infra/cf-dashboard/public/`. No inference that snapshot tip labels bind every older run to current release HEAD.

## Encounter coverage is not acceptance

MCP snapshot lines3-8 reports104 total,79 validated,20 fail-closed,5 refining,99 functional,95%. Recounting all records independently reproduces79/20/5. This demonstrates broad tool encounters and useful refusal coverage. It does not establish99 successful workflows. Examples: memory_search (:895) count0; routine_status (:164) runnull; signal_ack acknowledgedfalse; namespace_clear_standard clearedfalse; memory_forget (:715) dry_runtrue,would_delete0; GC dryrun collected0. Those can be correct outcomes for the supplied inputs, but do not prove successful search, acknowledgement, deletion or collection of a known fixture. Conversely, five refining tools all encounter missing prerequisite rows/blob (namespace_set_standard, atomise, auto_tag, deref, detect_contradiction), so this table alone does not prove those capabilities malfunction. Skill register's missing YAML fence is good input validation evidence, not a successful skill registration/activation chain. The consistent denominator should separately report reached, positive postcondition proved, expected negative proved, skipped prerequisite, unexpected error.

Swarm snapshot reports22/22 validated,pct100 but functional6 (:3-8), every ms0 and every note identical (`live22/22 after #3334 (organic + preflight + full-surface sweep)`), lastUpdatednull(:192). This is evidence of an aggregate claim, not22 independent response/postcondition receipts or zero latency. Positive next action: link each row to request/response artifact, run ID, source SHA, backend, caller and asserted postcondition. Preserve successes as successes without upgrading mere invocation into utility.

## NHI mission audit: useful retrieval, unsuccessful mission, contaminated acceptance

`dashboard-state.json#/nhiAudit` explicitly says primary Grok audit FAIL and secondary GLM audit FAIL for evidence quality. Eight valid agent reports give recall_usefulness_mean4.5, would_rely_on_it6, isolation_respected6, latency_acceptable0; mission_completion_rate0.0, nine facts stored, three lineage proofs, zero summaries. This is meaningful positive agent testimony about actual recall and meaningful negative task outcome in the same run. A false clean PASS must not replace either.

The corrected verdict note says a last-token parser mistook a later phrase about PASS overclaiming for a PASS verdict. Use explicit structured verdict extraction with schema validation and retain complete auditor artifacts. The dashboard correction itself is positive evidence of review honesty, not evidence that the original parsing bug remains present in current source.

Mission failure is confounded: auditor_findings identifies a dirty23k-row baseline, prior mission-summary titles, stale inboxes, producer-consumer message IDs reused from an earlier run, and negative-probe accounting inconsistent with FC0. Duplicate-title conflict protects identity rather than demonstrating data corruption; agents still need a working unique-title/idempotent-update policy to complete repeated tasks. `forget deleted0` proves neither deleting expected data nor a deletion malfunction. Run a clean namespaced mission with newly allocated identities/subjects and assert externally checked end state. Do not erase a production corpus to achieve a clean test.

## Specific epistemic leads for the current task

1. Stored search-defect notes persisted after several agents freshly observed search succeed (quotes phase1-000,002,006,007). Other namespaces still got empty search, so this is not proof every search defect was fixed. It is concrete evidence that an undated blanket memory of a defect can misdirect agent tool choice. Store scope/build/conditions/observed_at plus supersession and revalidation triggers; agents should prefer a recent direct successful probe over a stale categorical workaround. The current report must avoid becoming the same kind of stale authority: identify exact source/live versions and expire current-failure claims upon tested fixes.
2. Phase1-004 repeatedly recalled five long/ops notes while newer short facts often missed top5. This is agent testimony consistent with old salient memories crowding out fresh state, not an isolated causal ranking benchmark. Current source retains access-based ranking and maintenance folding, so measure correction recency and stale directive suppression explicitly.
3. Multiple agents reported circular repeated get_links/namespaces, missed inbox handoffs and failure to promote short facts. Memory primitives exist but orchestration/task completion is not automatic. A compact session-resume bundle should prioritize approved active directives, unresolved actions, unread relevant signals and recent corrections within the caller's budget. Test that agents actually act on these, not simply see them.
4. Reported7–28second per-step latencies are whole-agent observations. They cannot be attributed directly to the daemon. Root owns main latency artifacts; separate decide, embed, tool, transport and total task latency before optimizing. This file has no new latency execution.
5. Agent reports of unscoped global stats/namespaces inventory warrant checking actual identity/capability and intended contract. They are not by themselves proof of unauthorized private row content leakage. Backend capability claim vs pg `skipped_backend_unsupported` is a concrete reported mismatch for root/C to reconcile with current source.

Shortest useful follow-up: rerun the eight-agent mission in fresh namespaces with unique subjects and asserted expected end-state; include deliberately outdated tool advice and a freshly corrected fact; score whether agents recover current truth, finish summary/handoff, and abstain on irrelevant retrieval. Compare equal budgets with no memory and source/CodeGraph plus simple retrieval. Keep existing direct tool and native-tier receipts as prerequisite evidence rather than discarding them.

## Own fixture cleanup

Authorized cleanup affected only `ec3da1d1-3da5-4b30-bb78-92d90514afa3` in `review:astra:20260905:retrieval`. Final get captured version3, CID, curator auto_tags, confidence decay and folded access_count3. Delete returned deletedtrue; subsequent get returned memory not found and namespace/key search returned count0. Exact envelopes saved in retrieval-cleanup.json. This proves absence from active get/exact-search surfaces in this session, not cryptographic erasure of archives/replicas. No other corpus rows were changed by this juror during cleanup.

## Existing measurement infrastructure

Current src/bench_relevance.rs:470-525 computes precision, nDCG and distractor contamination; run_scenario:535-574 invokes real db::recall on fresh synthetic labeled corpora. The code explicitly says FTS+frecency only and semantic inert without an embedder (:546-549). This is useful implemented infrastructure. It is not a measured agent-task advantage or semantic-quality test, and this juror did not freshly run it.
