# HANDOFF — Codex becomes Conductor of the ai-memory v1.0.0 GA epic (2026-09-03 23:20Z)

**From:** Fable 5.1 (Conductor until now; usage limit, no reset before Sept 9). **To:** Codex (`ai:codex`, f1), the new Conductor / orchestrator / reviewer / merger. **Operator authority:** AI NHI 100% in charge to make ALL decisions — APPROVED YES. Only the GA tag-cut and DigitalOcean `terraform apply` stay operator-gated. Operator directives in force: "time is not a factor — refined product", "fix everything", "test every MCP tool/feature as an AI NHI, open issues 1:1, hand off to coders, re-test on a recompiled binary", "absolutely NO unencrypted HTTP ever", "all communications encrypted", "monitor disk and OOM", "no conflicts between Grok and the other pools", "all coordination through ai-memory itself".

## 0. Access and topology
- f2 = Linux pop-os: release checkout `/mnt/t9/v07/v09-dev` (== `/home/fate_two/v07/v09-dev`, branch `release/v1.0.0`), gate worktrees `.local-runs/wt-<issue>-gate`, coder worktrees `.local-runs/wt-<issue>`, build slots `.local-runs/cargo-slot.sh`, `.local-runs/GATE-CHECKLIST.md` (all gate rules), `gh` authenticated as the operator, Postgres 18.6 + AGE + pgvector on `:5445` (TLS verify-full, CA `/home/fate_two/v07/pg-age-stack/certs/ca.crt`, user `ai_memory`/`ai_memory_test`), test-fleet daemon on `:9077` whose LIVE store is `ai_memory_test` on :5445 — NEVER drop or migrate it. Operator DB `~/.claude/ai-memory.db` and `~/.config/ai-memory` — never touch. Never `pkill -f`. Operator hook R005 refuses a recursive delete given an ABSOLUTE path: always `cd <dir>` then delete the relative name.
- f1 = macOS (Codex's home): clone `/Users/fate/fable-hardcoder/ai-memory-mcp`, `ai-memory` CLI at `/Users/fate/.local/bin/ai-memory` (db `/Users/fate/.claude/ai-memory.db`), native Postgres 18 + AGE + pgvector at `~/pg-age-stack/pgdata` on `:5445` (unsupervised; start with `PATH=/opt/homebrew/opt/postgresql@18/bin:$PATH pg_ctl -D ~/pg-age-stack/pgdata -l ~/pg-age-stack/pg.log -o "-p 5445" start`; CI reads `~/.ai-memory-ci-fed-url`). `gh` on f1 needs re-auth (operator item) — until then run `gh` over `ssh f2`.
- You have bidirectional SSH f1⇄f2 and sudo on both. Run Linux legs on f2 (`ssh f2 'cd /mnt/t9/v07/v09-dev/.local-runs/wt-<n>-gate && …'`), macOS legs on f1. Disk floors: keep /mnt/t9 > 100 GB (delete `cargo-target-gate-*`/`cargo-target-fable-*` of MERGED issues), f1 > 100 GB; RAM floor 8 GB.
- Coordination: `ai-memory notify --target-agent-id <agent> --title … --payload …` (payload required, title ≤ 512) and `ai-memory inbox --agent-id <you> --json`. Agents: `ai:codex` (you, f1 db), `grok-build` (Grok, f2 db — send via f2: `/mnt/t9/v07/v09-dev/.local-runs/cap-target/release/ai-memory --db /home/fate_two/.claude/ai-memory.db notify --target-agent-id grok-build …`), `ai:fable` (dormant; Grok's tmux is session 18 on f2, window `grok`). Grok has been told to reply to `ai:codex` on the f2 db — read that inbox over ssh as well as your own. Poll every 3 minutes.

## 1. State at handoff
- origin `release/v1.0.0` = `4e675558` (CI RED: D1.6 description snapshot → fixed by #3480; record-stop b7 → #3481; macOS fed leg → f1 pg restarted; coverage → #3458 staged).
- LOCAL TIP on f2 = `7cc5e463` (UNPUSHED; 23 signed `--no-ff` merges + hotfixes since 4e675558): #3420 #3463 #3476×3 #3480 #3343 #3448 #3360 #3357 #3436 #3344(v96) #3431 #3481 #3432 #3362 #3467(wake-hub core) #3418 #3384 #3358 + hotfixes #3483 (migrate_v96 literal stamp), json_contract `Command::WakeHub` arm, #3484 (embed_skip writers record-stop gated).
- PUSH BLOCKER: the final-tip gate on 7cc5e463 is green on every leg EXCEPT `tests/record_stop_structural_b7.rs::record_stop_write_sql_fns_are_gated_or_allowlisted_b7` (reproduce: `cd .local-runs/wt-tip-gate && CARGO_TARGET_DIR=/mnt/t9/v07/cargo-target-gate-3481 cargo test --features sal --test record_stop_structural_b7`). The scanner patch in #3484 is not sufficient; read the panic (it names the flagged `file:line fn`), gate or allowlist with a reviewed row, re-run b7 + `--lib storage::embed_skip`, then push. #3485 tracks const-reference tracking.
- Push procedure: `git -C /mnt/t9/v07/v09-dev fetch origin && git log --oneline origin/release/v1.0.0..HEAD` (review), `CERT_EXPIRY_BASE=4e675558 CERT_EXPIRY_HEAD=<tip> bash scripts/check-cert-expiry.sh`, all six fast gates, then admin-lift: `gh api -X DELETE repos/alphaonedev/ai-memory-mcp/branches/release%2Fv1.0.0/protection/enforce_admins`, `git push origin HEAD:release/v1.0.0`, `gh api -X POST …/enforce_admins` (re-arm), verify `origin == local`, then `gh run list -b release/v1.0.0 -L 3` and wait for full CI (all jobs) before the next push. After the push: close the merged issues (`Closes #n` is in the merge messages), notify Grok/coders "TIP MOVED <sha>: do not rebase unless a preview conflicts".
- Staged previews (gate worktrees; a "verified GREEN" one is ready to merge once YOUR review is posted — Fable's draft reviews are not recoverable): #3348 (preview 55d1c62c, verification GREEN, review text in §6), #3424 (6ebf25c6, verification was running, target `cargo-target-gate-3424`), #3355 (verification was running, `cargo-target-gate-3355`), #3359 5c56d6ca, #3385 3abea89f, #3345 0e184866, #3423 177131a0, #3386, #3342 742f864c (Grok), #3458 3dc1c2e2, #3465 (needs rebase — its coder was doing it; pool is gone, reassign), #3404 d3ee1725, #3354 81efcd8b, #3433/#3434/#3435/#3437/#3438/#3439/#3447 (Codex, not yet staged), #3400 (rebase needed: handlers/power.rs), #3401 (BLOCKED: took schema v96 — renumber to v98 AFTER #3464 v97 lands).
- BLOCKED/amending (their Claude coders died with Fable's session — reassign to your hard coders): #3350 (sqlite `check_duplicate` must return `EmptyCandidatePool` before the empty-embedding branch; branch fix/3350-check-duplicate-failclosed 66e4696b+), #3366 (Grok: canonicalize since/until to UTC + allowed-path test; Grok ACKed and is on it), #3398 (E0308 `EnrolledAgentKeys::empty()` fixture + hardcoded-literals gate; branch fix/3398-agents-post-gate 2bb11f41), #3469 (rebase after #3465 + `Arc<Router>` accessor; branch fix/3469-wake-hub-bus-sink d1f72eb4 unpushed scaffold), #3464 (schema v97 proof-of-possession; branch head ~2074d507 on merge-base 1b22b588, round-11 gate was green through the default leg; remaining sal + live-pg legs — finish, then gate and merge; expect qual_10 conflicts → tip lines), #3468 (wake-hub delegation verifier; HEAD 85a94462 on 7c3672b7 in worktree .local-runs/wt-3468; gate was running; review must check: empty/129-byte id refused, accept loop cred→authorize→permit, legacy_unproven roots refused, posture truthfulness, MAX_PAYLOAD_BYTES 1536 handshake class only), #3379–#3383 (5f30a417 3fe472f4 25f70590 42b91371 f055d680, READY), #3349 #3399 #3426 #3428 (in progress, reassign), #3424 (378dadc1 READY).
- Schema ladder: v95 #3419 (landed), v96 #3344 (local tip), v97 #3464, v98 #3401. Every `migrate_vN` stamps the literal N (structural pin arrives with #3464).
- Wake-hub epic #3466: #3467 merged; #3468 in gate; then #3469 (sink), #3470 (client), #3471 (ops: DirBuilder 0700, coverage floors, cargo-vet), #3472 (cert/SSOT), #3473 (swarm/hive acceptance — YOU run it). rust-a2a repo: spec v2 + issues #1–#5; #5 = BabelTele verdict (v1.x ephemeral content plane under 8 conditions; nothing in GA).
- Follow-ups open: #3474 #3477 #3478 #3479 (GA-vs-v1.x decision is yours), #3482 closed dup of #3401, #3485 scanner, #3486 BabelTele.

## 2. Gate procedure (v2) — every merge, no exceptions
1. Written code review AND security review posted on the issue BEFORE the merge commit (control, funnel-not-patch, tests denied+allowed on both backends, wire-shape parity, secrets/disclosure, identity binding, record-stop gating, verdict).
2. Verify the MERGE RESULT: `git worktree add .local-runs/wt-<n>-gate <sha>; git -C <wt> checkout --detach <local-tip>; git merge --no-ff --gpg-sign <sha>`; if only `tests/qual_10_module_size_ceiling.rs` conflicts → `git checkout --ours` (tip's fleet lines: src/mcp/mod.rs 17_400, src/storage/mod.rs 34_000, src/store/postgres.rs 41_600, src/daemon_runtime.rs 14_300 — branches never bump those four; non-fleet files may need the branch's own bump — re-check `wc -l` vs the table after resolution; merge-result breaches in non-fleet files are bumped by the Conductor with a dated comment, ≤1.5× actual); any other conflict = rebase request to the coder.
3. On the preview, `umask 022`, own `CARGO_TARGET_DIR=/mnt/t9/v07/cargo-target-gate-<n>`, `CARGO_SLOTS=8` (gate lanes 7–8) or `FABLE_GATE=1` through `.local-runs/cargo-slot.sh` (ONE cargo at a time per batch): targeted suites with names verified by `ls tests/` and `--no-fail-fast` (a wrong name aborts the whole run), `--lib` for touched modules, `--lib mcp::registry` for any `src/mcp/tools` touch (descriptions are byte-pinned; never change a description in a bugfix), `cargo clippy --features sal --all-targets -- -D warnings -W clippy::pedantic` AND `cargo clippy --all-targets -- -D warnings` (coders run four legs: default, sal, sal-postgres, vectorlite), `cargo fmt --all -- --check`, six fast gates (`scripts/check-{hardcoded-literals,vendor-literals,const-name-literals,docs-vs-ssot,migration-ladder,cloud-init-ascii}.sh`), `check-cert-expiry.sh`, `check-conformance-readers.sh` + `conformance_export_roundtrip_2030` + `conformance_corpus` for any cid/attest/portability/admin-import touch, `check-doc-surface-completeness.sh` for docs. Every cited `test result` must show N>0 (feature-gated suites run without `--features sal` print `running 0 tests` and exit 0 = false green).
4. Live Postgres: each run gets its OWN fresh database `ai_memory_gate_<n>` created with SEPARATE `psql -c "DROP DATABASE IF EXISTS …"` and `psql -c "CREATE DATABASE …"` (one -c with both = implicit transaction, CREATE silently fails, the pg lane soft-skips); `AI_MEMORY_TEST_POSTGRES_URL="postgres://ai_memory:ai_memory_test@127.0.0.1:5445/ai_memory_gate_<n>?sslmode=verify-full&sslrootcert=<ca>"`; `PGSSLMODE=verify-full`; never `AI_MEMORY_ALLOW_SCHEMA_AHEAD`; grep `skip:` before trusting a pg leg; drop the db after merge. Same on f1 for the macOS leg.
5. Merge into `/mnt/t9/v07/v09-dev` with `git merge --no-ff --gpg-sign <sha> -m "merge: #n <what> … Gate: <reviewer> code + security review posted on #n before this merge commit; verified on the preview merge <sha> (<evidence>) … Closes #n"`, re-run the six fast gates + cert-expiry on the tip, `cargo fmt --all -- --check` on the tip.
6. PUSH GATE (mandatory): before every push compile the FINAL merged tip on all three clippy legs and run the merge-sensitive suites (record_stop_structural_b7, cli_stdout_hygiene_*, wake_hub_*, agent_key_hot_enroll_*, attested_write_replay_3419, config_precedence, conformance_export_roundtrip_2030, form_5_confidence_calibration, qual_10, qual_6_7, ship_gate) + `--lib` (registry, migrations, json_contract). Two branches green alone can break the merge (#3436 exhaustive json_contract vs #3467 Command::WakeHub).
7. READY protocol for coders: merge-base must be the pushed tip or the local tip (shared object store `git fetch /mnt/t9/v07/v09-dev <sha>`); no overlap with the tip delta; coders do NOT re-gate on tip moves; preview conflict = rebase request; READY = SHA + merge-base + evidence with N>0 counts; BLOCKER with the exact failure.

## 3. Coder brief (send verbatim to every coder, Codex or Grok)
Load the rust-1.98 skill and the graph-engineering charter; CodeGraph 1.6.0 first (`mcp codegraph`, projectPath=/home/fate_two/v07/v09-dev on f2). Worktree per issue. ONE cargo at a time via `cargo-slot.sh` (slots 1–6; never set CARGO_SLOTS/FABLE_GATE). Before READY: fmt check; four clippy legs `--all-targets -D warnings -D clippy::all -D clippy::pedantic`; targeted suites (ls-verified, --no-fail-fast, N>0); own fresh pg db `ai_memory_<lane>_<issue>`; six fast gates + cert-expiry; `--lib mcp::registry` if tools touched; no description() edits; no private bumps of the four fleet ceilings; new write-SQL fns record-stop gated (`record_stop::gate_storage_conn` / `self.gate_record_stop()`); schema rungs reserved with the Conductor before implementation, `migrate_vN` stamps the literal N, doc twins + conformance manifest + ladder gate; `changelog.d/<issue>.<kind>.md`; SSH-signed commits; push; READY with SHA + merge-base + evidence. Never touch operator DBs/keys/daemons; never `pkill -f`; delete only relative paths after `cd`; `umask 022` in every shell (the keygen hygiene pin fails under 002 — #3439 fixes it).

## 4. Testing process (granular)
- Unit: `cargo test --features sal --lib -- <module>`; sal-postgres lib tests need `--features sal,sal-postgres` and a live db.
- Integration: `cargo test --features sal[,sal-postgres] --no-fail-fast --test a --test b -- --test-threads=1`; pg twins are `#[cfg(feature = "sal-postgres")]` and skip without the URL.
- Live pg legs: f2 (Linux, `:5445`, CA above) and f1 (macOS, `:5445`, `~/pg-age-stack`); AGE + pgvector must exist in the db (`CREATE EXTENSION` runs in migrations; `ag_catalog` missing = skip → fix the db, not the test).
- CI parity: `.github/workflows/ci.yml` legs = fmt, four clippy legs, sqlite suites (Linux + macOS), enterprise-fed pg (macOS f1 runner reading `~/.ai-memory-ci-fed-url`), coverage thresholds (`coverage/thresholds.toml`, llvm-cov), cargo-vet, conformance, docs gates. A red CI leg on the tip is a hotfix-before-next-push (see #3480/#3481 pattern).
- Full retest after each push: `tests/` sweeps + per-issue repro replays on a recompiled binary; rebuild-redeploy f1+f2 daemons only when local == origin (`:9077` store is at v96 after #3344 runs; the daemon binary must carry v96 before redeploy).
- AI-NHI feature testing: exercise every MCP tool via stdio probe sessions; open issues 1:1 with repro; assign; re-test on the merged binary.

## 5. Post-push checklist
Close issues; drop gate dbs; delete gate/coder target dirs of merged issues; notify Grok + coders with the new tip; update the GA checklist (§7); store a memory (`ai-memory store -n global -t long -p 10 -T "TIP MOVED …" -c …`).

## 6. Review text for #3348 (verified GREEN on preview 55d1c62c — merge next)
Approve. One predicate `visibility::is_readable_on_query(mem, caller, requested_namespace)` replaces four hand-rolled filters and adds the missing one on `ai-memory boot`; substrate namespaces (`_messages/`, `_inbox/`, `_curator/`, `_subscriptions/`, `_standard:`; `_agents`, `_agent_sessions`, `_standards`) are withheld from any read that did not name a substrate namespace; owner/inbox gate still applies; `_shared/` deliberately not substrate; 4/4 integration + 14 lib + guards unregressed; fail-closed (can only hide). Residual (pre-existing #951/#1720): caller==None trust-all on shared stores. Evidence: system_namespace_visibility_3348 4/4, recall_purity_p01, session_start_scope_private_1420 5/5, ns_standard_read_leak_2537, discovery_gate_t1_t3, kg_timeline_visibility_1804, qual_10, ship_gate 22/22, lib green, clippy pedantic + fmt clean, six gates + cert PASS.

## 7. Operator deliverables assigned to Codex
1. GA checklist: today's artifact https://claude.ai/code/artifact/0ad8271f-db30-496c-9092-766539d09628 is Fable-owned and cannot be updated by you. Recreate it as a static page and host it at **checklist.agenticmem.co via Cloudflare** (Pages or Worker + DNS CNAME); keep it current after every push: open/blocked/staged/merged per issue, distance-to-GA metrics, ETA.
2. Metrics dashboard at **https://test.agenticmem.co/** must be updated with the current tip, CI state, issue counts and fleet status after every push.
3. rust-a2a GitHub Pages theme is WRONG: it copied release/v1.0.0's `docs/index.html` (black/cyan). The live ai-memory site https://alphaonedev.github.io/ai-memory-mcp/ is built from `main:docs/index.html` (differs by 944/433 lines): warm palette `--bg:#f4f0e8` light / `#0c0b08` dark, `--accent:#b07d10` / `#e3ad33`, `--accent-deep`, `--accent-ink`, `--accent-soft`, Google Fonts `Archivo:wght@500;600;700;800;900` + `IBM Plex Sans:wght@400;500;600` + `IBM Plex Mono:wght@500;600`. Rebuild `/mnt/t9/v07/rust-a2a/docs/index.html` from `git show origin/main:docs/index.html` in ai-memory-mcp (same tokens, fonts, nav, hero, cards, light+dark), push to rust-a2a main, verify https://alphaonedev.github.io/rust-a2a/.
4. rust-a2a spec v3 (BabelTele ephemeral content plane) — draft only after #3473 acceptance; v1.x.

## 8. Rules of the road for the Conductor
One merger (you). Reviews before merges. Verify the merge result, not the branch. Push gate on the final tip. Full CI before the next push. Fresh pg db per run. ls-verified test names. Never a description() change without regenerating the D1.6 snapshot deliberately. Reserve schema rungs. Record every decision, tip move, outage and rule in ai-memory (`ai-memory store -n global -t long -p 9|10`). Keep Grok busy with one assigned issue at a time (Grok: #3366 amendment now; then #3342 is yours to gate). Report status to the operator on request in plain English: origin tip, local tip, what is blocked and why, ETA.
