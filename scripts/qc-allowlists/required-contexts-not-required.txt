# required-contexts-not-required.txt — the DELIBERATELY-UNREQUIRED job ledger
# for the gating workflows, enforced by rule (f) of
# `scripts/check-required-contexts.sh` (#2636).
#
# ============================================================================
# *** THIS FILE IS CURRENTLY EMPTY, AND THAT IS THE PASSING STATE. ***
# ============================================================================
# Empty means every job in every gating workflow is DECLARED REQUIRED in
# `required-contexts-release.txt`. The teeth are in the rule, not the contents:
# from now on a newly-added job in a gating workflow must be declared one way
# or the other, and cannot DEFAULT to unenforced.
#
# WHY THE RULE EXISTS (#2636). Four `c8-precheck.yml` integrity gates ran on
# every pull request and were required by nothing — including
# `Required-context + classify-base soundness gate (#2494/#2496/#2508)`, the
# sole mechanical proof that the OTHER required contexts are creatable,
# reachable, non-skipping, non-cancelled and non-truncated. A pull request that
# broke the gate that proves the gates work could merge. Worse, the prose
# "KNOWN GAPS" note that was supposed to record this named only TWO of the four
# and had gone stale without anyone noticing — which is the argument for a
# machine-parsed ledger over a comment.
#
# SCOPE, and why it is not repo-wide. Rule (f) covers the workflows declared in
# `COVERED_WORKFLOWS` in the gate: `ci.yml`, `c8-precheck.yml`, `coverage.yml`,
# `cert-postgres-age.yml`, `postgres-ignored.yml` (#3552 added the last two).
# Those are the workflows that carry required contexts — the PR gating pipeline.
# Rules (d) and (e) ARE repo-wide, and the difference is deliberate: (d) and
# (e) detect STATIC DEFECTS that are wrong wherever they appear, whereas (f)
# encodes a POLICY JUDGEMENT ("should this job be required?") that has to be
# authored per workflow. Sweeping `release.yml`, `publish-sdks.yml`,
# `yank.yml` and the rest into it would mean inventorying ~45 jobs that never
# fire on `pull_request` at all — a junk drawer that teaches readers to skim
# past the file, which is how a ledger stops being read. The gate hard-fails if
# a mirror context ever appears in a workflow OUTSIDE this list, so the scope
# cannot silently narrow while the required set widens.
#
# FORMAT
#   <workflow-file> <job-id> <YYYY-MM-DD> #<issue> [free-text note]
#
#   * The key is the JOB ID, not the job name: ids have no spaces, and a job
#     renamed for readability should not silently drop its entry.
#   * The DATE is when the decision was taken. The ISSUE is what closes it.
#     Both are mandatory and format-checked, so an entry can never rot into an
#     unexplained suppression.
#   * A line whose first non-space character is `#` is a comment.
#
# STALE ENTRIES HARD-FAIL, in both directions:
#   * an entry naming a workflow/job that no longer exists, and
#   * an entry for a job whose context IS declared in the mirror — a job cannot
#     be required and deliberately-unrequired at once, and letting that pass
#     would leave a line that could pre-absolve a FUTURE job reusing the id.
#
# THIS IS NOT AN ABSOLUTION. An entry says "we looked, and decided not to
# require this job, on this date, tracked here". The correct disposition for an
# integrity gate is almost always to REQUIRE it: add its exact reported name to
# `required-contexts-release.txt` instead, then make the branch-protection call
# (mirror FIRST, protection after — the ORDER doctrine in that file's header).
#
# ---------------------------------------------------------------------------
c8-precheck.yml create-extension-allowlist-gate 2026-08-06 #2648 supply-chain CREATE EXTENSION allowlist gate; deliberately unrequired at introduction. The correct disposition for an integrity gate is to REQUIRE it (add its reported name to required-contexts-release.txt + the branch-protection API, mirror FIRST), which is the tracked follow-up when the required set is next revised; declared here so rule (f) stays green in the interim rather than defaulting to unenforced.
c8-precheck.yml commit-signing-posture-gate 2026-08-11 #2486 control-integrity Commit-signing posture gate; deliberately unrequired at introduction. The correct disposition for an integrity gate is to REQUIRE it (add its reported name — "Commit-signing posture gate (#2486)" — to required-contexts-release.txt + the branch-protection API, mirror FIRST), which is the tracked follow-up when the required set is next revised; declared here so rule (f) stays green in the interim rather than defaulting to unenforced. Runs unconditionally on every PR and HARD-FAILS loudly on violation; only branch-protection ENFORCEMENT (a live GitHub ruleset mutation, Sensitive-class) is deferred, not the check itself.
c8-precheck.yml const-name-literal-gate 2026-08-23 #3122 const-name-literal identifier gate (#3121); deliberately unrequired at introduction. The correct disposition is to REQUIRE it (add its reported name — "Const-name-literal identifier gate (#3121)" — to required-contexts-release.txt + the branch-protection API, mirror FIRST). Declared here so rule (f) stays green rather than defaulting to unenforced. Runs unconditionally on every PR.
c8-precheck.yml test-keydir-mode-gate 2026-09-14 #3737 test key-dir mode gate (#3733): a test that bare-creates a directory and passes it as the key dir is refused (must use tests/common/key_dir_sandbox.rs::mkdir_0700); deliberately unrequired at introduction because requiring it means editing release/v1.0.0 branch protection, an operator action affecting every PR, which the GA freeze does not widen. The job runs and reports on every PR and blocks nothing until #3737 promotes it (mirror FIRST, protection after). This is a decision record, not an absolution.
# ---------------------------------------------------------------------------
c8-precheck.yml foreign-text-to-caller-gate 2026-09-13 #3688 gate 7 foreign-text-to-caller (SOURCE x SINK, provenance-walked) gate; deliberately unrequired at introduction. The correct disposition is to REQUIRE it (add its reported name -- "Foreign-text-to-caller gate (#3688 gate 7)" -- to required-contexts-release.txt + the branch-protection API, mirror FIRST per the #3554 ORDER rule). Declared here so rule (f) stays green rather than defaulting to unenforced. Runs unconditionally on every PR and HARD-FAILS on any unledgered site; the 311 live instances / 170 keys on the chain-12 tree are held in foreign-text-to-caller.txt under #3713/#3707/#3724/#3708/#3674/#3698.
c8-precheck.yml declaration-hash-gate 2026-09-18 #3557 Declaration hash gate (#3557); deliberately unrequired at introduction per the Conductor ruling on b4d40cb03: the context cannot report on release PRs until the #3769 promotion lands, and check-required-contexts-live.sh refuses a declaration that runs ahead of live protection (#3554 / #2712). The lockstep — live protection, then --pin-from-live, then the required-contexts-release.txt line "Declaration hash gate (#3557)" — is ONE later Conductor commit, at which point this entry is removed. Runs unconditionally on every PR and HARD-FAILS on D1-D4; only branch-protection ENFORCEMENT is deferred, not the check.
