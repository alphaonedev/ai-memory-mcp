# CallerContext::for_admin("<literal>") privacy-bypass allowlist (C8 safeguard).
# Auto-generated by scripts/qc-codegraph-precheck.sh --update.
# Format: <repo-root-relative-file>:<literal>
# Edit deliberately — every entry should be a reviewed exception.
# See CLAUDE.md §"Enforceable Orchestrator Safeguards" (C8).

src/cli/agents.rs:DAEMON_PRINCIPAL
src/cli/commands/undo_edit.rs:AI_OPERATOR
src/cli/curator.rs:AI_CURATOR
src/cli/stop.rs:AI_OPERATOR
src/curator/compaction.rs:CONSOLIDATOR_AGENT_ID
src/curator/decorrelation_probe.rs:agent_id
src/curator/reflection_pass.rs:agent_id
src/curator/transcript_classify_pass.rs:agent_id
# FBL-22 (#2342) — postgres serve maintenance loop (spawn_postgres_maintenance_
# loop_if_enabled): the periodic archive_purge must cover EVERY owner's stale
# archives, mirroring the sqlite loop's unscoped db::auto_purge_archive. The
# admin/bypass ctx is the daemon principal, constructed from the DAEMON_PRINCIPAL
# const (never a raw literal); background maintenance, never a tenant handler.
src/daemon_runtime.rs:DAEMON_PRINCIPAL
src/daemon_runtime.rs:EMBEDDING_BACKFILL
src/federation/receive.rs:FEDERATION_CATCHUP
# #2447 (CWE-284) — postgres federation-receive namespace-scope probe
# (`resolve_stored_namespace`): resolves a row's namespace by id ONLY to render
# a REFUSAL on the inbound write / delete lanes; the row is never returned to
# the peer. The bypass is REQUIRED for correctness, not convenience —
# `PostgresStore::get` folds a #910 visibility denial into `NotFound`, so a
# tenant-scoped ctx would report a hidden foreign row as "no such row", which
# is fail-OPEN on exactly the rows the gate protects. Peer/operator surface,
# never a tenant handler; mirrors the sqlite twin's unscoped scalar read.
src/handlers/federation_signing_check.rs:probe_principal
# #3075 (lane L-PGP) — the postgres federation-RECEIVE APPLY context
# (`federation_apply_ctx`), used by the migrated `/sync/push` subcollections
# (namespace_meta / namespace_meta_clears, and the further families as they
# land). PARITY, not convenience: the sqlite twin applies these lanes through
# raw `db::*` free functions that consult NO caller identity at all — the
# federation authorization is the PEER-SCOPE gate (`receive_auth::inbound_*_
# authorized`) plus the per-lane crypto attestation, never the local
# owner/tenant gate. Several pg trait methods DO carry a local caller-owns gate
# for their TENANT surfaces (`clear_namespace_standard` #1777/#2545,
# `archive_by_ids` #3193, `archive_restore` #3271); routing a federated apply
# through a tenant-scoped ctx would make the pg receiver refuse rows the sqlite
# receiver applies — a silent cross-backend divergence of exactly the #2488
# shape, whose symptom is two peers answering `get_standard` with different
# governance. Every call site runs AFTER the lane's `receive_auth` verdict, on a
# peer/operator surface (#238 envelope + #29 sig + #30 nonce + #43 enrollment
# all precede it), never a tenant-facing handler; the principal is the attested
# sender so the write stays attributable. Adds NO reach a peer did not have.
# Security approval: Deputy Conductor Fable 5.1 (f1), 2026-09-05 — parity with the
# owner-blind sqlite receive loop; every call site runs after the lane's receive_auth
# verdict on a peer/operator surface.
src/handlers/federation_signing_check.rs:receive_principal
src/handlers/admin.rs:DAEMON_PRINCIPAL
# #3464 (v1.0.0, security-high) — proof-of-possession bind challenge issuer
# (`issue_bind_challenge`): the durable single-use challenge row is written with
# the DAEMON_PRINCIPAL admin ctx (bound to the local `issuer` name) because the
# challenge table is substrate enrolment state, not tenant data; the route
# (`POST /api/v1/agents/{id}/pubkey/challenge`) is `require_admin`-gated ahead of
# this call, the candidate key is issuer-pinned in the row, and the bind that
# consumes it re-checks the (agent, key) tuple under the same admin gate. Adds no
# read privilege. SECURITY APPROVAL 2026-09-05 (Fable, #3464 review): JUSTIFIED.
src/handlers/admin.rs:issuer
src/handlers/hook_subscribers.rs:AI_HTTP_INTERNAL
src/handlers/http.rs:AI_HTTP_INTERNAL
# #3064 batch C — POST /api/v1/memory_export_reflection postgres SAL
# arm. Read-only UNFILTERED export matching sqlite `db::get` (no
# caller-ownership gate by design, #1075-era). `for_admin` adds NO new
# privilege vs the already-allowlisted sqlite path. SECURITY APPROVAL
# 2026-08-29 (Fable, d2d34b6e): JUSTIFIED. Raw literal (not a sentinel)
# because the principal is HTTP-route-scoped, not a daemon identity.
src/handlers/route_1111.rs:http:export-reflection
src/handlers/subscriptions.rs:SUBSCRIPTION_DISPATCH
src/migrate.rs:AI_MIGRATE
src/cli/stop.rs:AI_OPERATOR
# #2121 — L2 transcript-recovery walker (recover_from_transcript_store): a
# substrate re-store reading LOCAL host transcripts (CLI recover-previous-
# session + session boot only; never a tenant-facing handler). The bypass
# ctx keys the covenant clause-1 substrate why_trace stamp, matching the
# direct-sqlite path's substrate_authored=true.
src/recover/mod.rs:opts.agent_id
